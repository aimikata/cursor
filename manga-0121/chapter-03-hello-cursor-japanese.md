# 第3章：最初のコード：今日やったことを1行でログに残す（確認用日本語訳）

> **注意：** これは確認用の日本語訳です。最終出版は英語版です。
> ストーリーの流れと内容の正確性を確認するために使用してください。

## 📖 章の概要

**章タイトル：** 最初のコード：今日やったことを1行でログに残す  
**ページ数：** 18-20ページ（視覚学習に最適化）  
**学習目標：**
- **何を作るか・なぜ作るか**を理解してから書き始める
- **毎日やらないといけない、簡単だけど少し面倒**な「今日やったこと」メモを、1行でログに追記するスクリプトを作る
- Cursorでプロジェクトを開き、コードを書いて実行し、Tabの補完を活用する

---

## 🎬 ストーリー構造

### **オープニングシーン（ページ1-2）：何を作る？ なぜ作る？**

**【マンガパート】**

**設定：** アレックスのアパート。Cursorのインストールと設定が終わり、手元にメモ用のテキストファイルを開いている。

**マンガシーン1（ページ1）：**
- **パネル1：** アレックスが「今日やったこと」をメモしようと、テキストファイルを開いている。日付を打とうとして手が止まる。
- **アレックス（思考）：** 「毎日、今日やったことを残したいんだけど…。ファイル開いて、日付を打って、内容書いて、保存。**簡単なんだけど、ちょっと面倒**で続かない。日付のフォーマットも揺れるし。」

**パネル2：** デイビッドがビデオ通話で現れる。
- **デイビッド：** 「それ、コードでラクにできるよ。**『やったこと』を1行渡すだけで、日付つきでログに追記する**スクリプトを作ろう。毎日やることだし、**少しの手間を省く**だけで続けやすくなる。」
- **アレックス：** 「毎日やらないといけない、簡単だけど面倒、ってまさにそれ。**何のため**かはっきりしてる。」

**パネル3：** アレックスが興味を持って身を乗り出す。
- **デイビッド：** 「**ゴール**をはっきりさせよう。作るものは『今日やったことを1行でログに追記するスクリプト』。入力は**やったこと**（例：`"Chapter3の下書きを書いた"`）、出力は**日付＋時刻＋その1行**がログファイルに追記される。ターミナルで`python log.py "〇〇"`と打つだけ。」
- **アレックス：** 「目的がわかってるから、やる気出る。やってみます。」

**【インフォグラフィックパート】**

**ページ1-2、下部：**
- **この章で作るもの：** 今日やったことを1行でログに追記するスクリプト
- **補足：** 毎日「今日やったこと」をメモするのは**簡単**だが、ファイルを開いて日付を打って…と**少し面倒**で続かない人が多い。このスクリプトで、**1行渡すだけ**で日付つき追記になる。
- **入力：** コマンドの引数で「やったこと」（例：`python log.py "Chapter3の下書きを書いた"`）
- **出力：** `daily_log.txt` に `2025-01-21 14:30 Chapter3の下書きを書いた` のように追記
- **あとで使える：** 日付指定でその日の行だけ抽出、ログの保存先を変える、など

---

### **セクション3.1：プロジェクトを作成する（ページ3-5）**

**【マンガパート】**

**マンガシーン2-1（ページ3）：**
- **パネル1：** アレックスがデスクトップ（またはドキュメント）に`daily_log`フォルダを作成する。
- **デイビッド：** 「まずはプロジェクト用フォルダを作って、Cursorで開く。名前は`daily_log`でOK。場所はどこでも好きなところで。」

**パネル2：** Cursorで「File」→「Open Folder」を選び、`daily_log`を開く。
- **デイビッド：** 「Cursorで『Open Folder』→`daily_log`を選択。`⌘+O`（Mac）や`Ctrl+K Ctrl+O`（Windows）のショートカットでも開けるよ。」
- **視覚的表現：** 左サイドバーに`daily_log`が表示され、まだ中身が空。

**パネル3：** アレックスがEXPLORERで`daily_log`を右クリックし、「New File」で`log.py`を作成する。
- **デイビッド：** 「この中に、ログ追記用のPythonスクリプト`log.py`を1つ作る。実行するときはターミナルで`python log.py "やったこと"`のように、**やったことを引数で渡す**形にするよ。」

**【インフォグラフィックパート】**

**ページ3-4：**
- **プロジェクトの開き方（そのまま再現できる手順）：**
  1. デスクトップやドキュメントなど、好きな場所に`daily_log`フォルダを作成する
  2. Cursorでメニュー「**File**」→「**Open Folder**」をクリックする（または`⌘+O`／`Ctrl+K`のあと`Ctrl+O`）
  3. 開いたダイアログで`daily_log`フォルダを選び、「**Open**」（または「フォルダの選択」）をクリックする
  4. 左のEXPLORERに`daily_log`が表示される。中身が空の状態でOK
  5. EXPLORERの`daily_log`を**右クリック**→「**New File**」→ ファイル名に`log.py`と入力してEnter
- **ポイント：** フォルダ＝1つのプロジェクト。ここに`log.py`を置き、`daily_log.txt`も同じフォルダに作られる。

---

### **セクション3.2：コードを書く―何のための入力か理解する（ページ6-10）**

**【マンガパート】**

**マンガシーン3-1（ページ6）：**
- **パネル1：** アレックスが`log.py`に`import sys`と`from datetime import datetime`を書く。
- **デイビッド：** 「`sys`でコマンドの引数（やったこと）を受け取る。`datetime`でいまの日付・時刻を取って、ログの行頭に付ける。」
- **アレックス：** 「入力はやったこと、出力は日付つきでファイルに追記、ですね。」

**パネル2：** アレックスが`if len(sys.argv) < 2:`で「使い方」を表示する処理を書き、`line = " ".join(sys.argv[1:])`で引数を受け取る。
- **デイビッド：** 「`sys.argv[1]`以降がやったこと。`python log.py "A B"`なら`A B`が1つの文字列になる。`" ".join`で複数の単語も1行にまとめられる。」
- **視覚的表現：** ターミナルで`python log.py`だけ実行したとき、「Usage: python log.py "やったこと"」と出る様子。

**パネル3：** アレックスが`with open(log_file, "a", encoding="utf-8") as f:`と書き、`f.write(f"{now:%Y-%m-%d %H:%M} {line}\n")`で追記する。Tabで`datetime`の書式や`"\n"`が提案される。
- **デイビッド：** 「`"a"`は追記モード。同じファイルにどんどん足していく。`encoding="utf-8"`で日本語も正しく書ける。」
- **アレックス：** 「Tabの提案で`%Y-%m-%d`の続きが出て、意味を確認してから受け入れた。」

**【テキストパート - コードのみ】**

**ページ6-7、下部：**
```python
# log.py - 今日やったことを1行でログに追記する
import sys
from datetime import datetime

def main():
    if len(sys.argv) < 2:
        print("Usage: python log.py \"やったこと\"")
        print("Example: python log.py \"Chapter3の下書きを書いた\"")
        return
    line = " ".join(sys.argv[1:])
    now = datetime.now()
    log_file = "daily_log.txt"
    with open(log_file, "a", encoding="utf-8") as f:
        f.write(f"{now:%Y-%m-%d %H:%M} {line}\n")
    print(f"Logged: {now:%Y-%m-%d %H:%M} {line}")

if __name__ == "__main__":
    main()
```

**【テキストパート - 詳細説明】**

**ページ7-8、下部：**
- **ターミナルで実行する完全手順（そのまま再現できる）：**
  1. **ターミナルを開く：** メニュー「**Terminal**」→「**New Terminal**」をクリックする（または`` Ctrl+` ``（バッククォート）を押す）。画面下部にターミナルパネルが開く。
  2. **プロジェクトフォルダに移動：** ターミナルで`cd daily_log`と入力してEnter（`daily_log`フォルダがCursorで開いている場合、既にそのフォルダにいることもある。`pwd`で現在の場所を確認できる）。
  3. **スクリプトを実行：** `python log.py "CursorのChapter3を読んだ"`と入力してEnter。**引用符（`"`）で囲む**のを忘れずに。
  4. **結果を確認：** ターミナルに「Logged: 2025-01-21 14:30 CursorのChapter3を読んだ」と表示される。左のEXPLORERに`daily_log.txt`が現れ、クリックして開くと、日付＋内容の1行が入っている。
  5. **もう一度試す：** `python log.py "2回目のテスト"`などで、**追記**されることを確認する（`daily_log.txt`に2行目が追加される）。

**【マンガパート】**

**マンガシーン3-2（ページ7）：**
- **パネル1：** デイビッドが`"a"`（追記）と`encoding="utf-8"`の説明をする。
- **デイビッド：** 「`open(..., "a")`で**追記**。ファイルがなければ新規作成、あれば末尾に足す。`"w"`だと上書きになっちゃうから気をつけて。」
- **アレックス：** 「毎日足していくから、追記が大事ですね。」

**パネル2：** アレックスがターミナルで`python log.py "CursorのChapter3を読んだ"`と実行する。
- **デイビッド：** 「まずは**すぐ試せる**ように、今やったことを1行で渡して実行してみて。`daily_log.txt`ができて、1行追記されるはず。」
- **視覚的表現：** ターミナルに「Logged: 2025-01-21 14:30 CursorのChapter3を読んだ」と表示。EXPLORERに`daily_log.txt`が現れ、開くと日付＋内容の1行が入っている。**ターミナルは**メニュー「Terminal」→「New Terminal」で開くか、`` Ctrl+` ``（バッククォート）でも開ける。

**パネル3：** アレックスが`daily_log.txt`を開いて中身を確認する。
- **アレックス：** 「日付と時刻が自動で付いて、1行で残った。毎日これやるなら、ファイル開いて日付打つよりずっとラク。」
- **デイビッド：** 「そう。**毎日やらないといけない、簡単だけど少し面倒**なことを、ちょっとしたコードで続けやすくするのが目的だね。」

**【インフォグラフィックパート】**

**ページ7-8：**
- **やっていることの整理：**
  - **入力：** コマンド引数で「やったこと」（例：`python log.py "〇〇した"`）
  - **処理：** 現在の日付・時刻を取得し、`daily_log.txt`に`YYYY-MM-DD HH:MM やったこと`の形で1行追記。
  - **出力：** ログファイルへの追記。ターミナルに「Logged: …」と表示。
- **ポイント：** 毎日、何かやったら`python log.py "〇〇"`と打つだけでOK。日付を手で打つ手間がなくなる。

**【テキストパート - 詳細説明】**

**ページ8、下部：**
- **ログを実行して確認する手順（そのまま再現できる）：**
  1. Cursorで`daily_log`フォルダを開いた状態にする
  2. ターミナルを開く：メニュー「**Terminal**」→「**New Terminal**」、または`` Ctrl+` ``（バッククォート）
  3. ターミナルで`cd`コマンドは不要（すでにプロジェクトフォルダが開いているため）。`python log.py "ここにやったことを書く"` と入力する
  4. Enterキーで実行する。ターミナルに「Logged: 日付 時刻 やったこと」と出ればOK
  5. 左のEXPLORERで`daily_log.txt`をクリックして開き、追記された1行を確認する
- **注意：** `log.py`を編集したあとは、**保存**（`⌘+S`／`Ctrl+S`）してから実行すること。

---

### **セクション3.3：Tabで補完しながら書き足す（ページ9-12）**

**【マンガパート】**

**マンガシーン4-1（ページ9）：**
- **パネル1：** デイビッドが「もう少し便利にしよう。『次に試せること』で、**その日の行だけ取り出す**オプションを足せるよ」と提案する。
- **アレックス：** 「今週やったことだけ見たい、とかにも使えそう。」

**パネル2：** アレックスが「Tab、便利です。ただ、なんでもかんでも受け入れていいわけじゃないですよね？」と聞く。
- **デイビッド：** 「そう。**あくまで助け**。必ず目で確認して、おかしかったらEscで却下して自分で直す。Chapter 1でも話した通り、良い提案だけ受け入れるクセをつけるといい。」

**パネル3：** アレックスが「引数なしで実行したとき、Usage の代わりにログの最後の5行を出したりできますか？」と聞く。
- **デイビッド：** 「できるよ。`len(sys.argv) < 2`のとき、ファイルを読んで末尾数行を表示する処理を足せばいい。『次に試せること』にしておこう。」

**【インフォグラフィックパート】**

**ページ9-10：**
- Tabを使うときの心がけ：
  - ✅ 提案を**必ず目で確認**してから Tab で受け入れる
  - ✅ おかしいと思ったら **Esc** で却下して自分で書く
  - ✅ **何のためのコードか**を意識しながら書く
  - ❌ 考えずに全部 Tab で受け入れるのは避ける

---

### **「次に試せること」と広げ方（ページ11-12）**

**【マンガパート】**

**マンガシーン4-2（ページ11）：**
- **パネル1：** アレックスが「週末に、その週やったこと振り返りたいんです」と言う。
- **デイビッド：** 「じゃあ、**日付を引数で渡して、その日の行だけ抽出して表示する**オプションを足せるよ。`python log.py --day 2025-01-21`みたいに。」

**パネル2：** アレックスがメモを取っている。
- **アレックス：** 「毎日`python log.py "〇〇"`で足していって、振り返るときは`--day`でその日だけ見る、って感じですね。」
- **デイビッド：** 「そう。**毎日やる簡単だけど面倒なこと**をラクにして、その結果をあとで活用できる。作ったものが実際に使えるとモチベーションも続く。」

**【インフォグラフィックパート】**

**ページ11-12：**
- 次に試せること：
  - **その日の行だけ抽出：** `python log.py --day 2025-01-21` で、`daily_log.txt`からその日の行だけ表示する。
  - **引数なしで最後の5行表示：** 実行時に何も渡さなければ、直近5行を表示する。すぐ「最近何やったっけ？」を確認できる。
  - **ログの保存先を変える：** `log_file`を`Documents/logs/daily_log.txt`などに変更。フォルダがなければ作成する処理を足す。
- **ポイント：** 目的がはっきりしてるから、**ちょっとした拡張**がしやすい。

---

### **章のまとめ（ページ13-14）**

**【マンガパート】**

**マンガシーン5（ページ13）：**
- **パネル1：** アレックスが`daily_log`フォルダと`log.py`、`daily_log.txt`を見て満足そうにしている。
- **アレックス：** 「Hello Worldじゃなくて、**毎日やる簡単だけど面倒なこと**をラクにするスクリプトでよかった。作って終わりじゃなく、自分で使い続けられそう。」
- **デイビッド：** 「その『何のためか』を最初に置くの、大事だよ。次からも、**何を作るか・何のための入力か**を意識して進めよう。」

**パネル2：** デイビッドが章のポイントをまとめる。
- **デイビッド：** 「この章では、(1) プロジェクト用フォルダを作ってCursorで開く、(2) `log.py`で**今日やったことを1行でログに追記する**ゴールに向かってコードを書く、(3) ターミナルで`python log.py "やったこと"`のように引数渡して実行、(4) Tabで補完しながら書き足す、をやったね。」
- **アレックス：** 「はい。毎日やることだから、目的がわかってて続けやすそうです。」

**【インフォグラフィックパート】**

**ページ13-14：**
- 第3章のまとめ：
  - ✅ **何を作るか・なぜ作るか**を最初に決める
  - ✅ **毎日やらないといけない、簡単だけど少し面倒**な「今日やったこと」メモを、1行でログに追記するスクリプトを作成し、実際に実行して試す
  - ✅ プロジェクトフォルダの作成、`log.py`、ターミナル実行、Tabの基本を体験する
  - ✅ 作ったものを**毎日のルーチン**として使い、実用性を感じる
- **実践チェックリスト（今すぐ試す）：**
  - [ ] `daily_log`フォルダを作り、Cursorで「File」→「Open Folder」で開く
  - [ ] `log.py`を作成し、本章のコードをコピーして貼り付けて保存する
  - [ ] ターミナルを開き、`python log.py "Chapter3を読んだ"` を実行する
  - [ ] 「Logged: …」と表示され、`daily_log.txt`に1行追記されることを確認する

**【マンガパート】**

**マンガシーン5-2（ページ14）：**
- **パネル1：** アレックスとデイビッドが次の章に向けて会話する。
- **アレックス：** 「次はどんなことをしますか？」
- **デイビッド：** 「Chapter 4では、ファイルが増えて『この処理、どこのファイルにあったっけ？』となったときに、**Cursorの検索**の使い方をやる。コードを意味で探せるから、結構便利だよ。」

**【インフォグラフィックパート】**

**ページ14：**
- 次章の予告：
  - **Chapter 4: 賢い検索でコードを見つける**
  - プロジェクト内のコードを「意味」で検索する
  - 実践的なプロジェクト例で検索のやり方を学ぶ

---

## ✅ 確認ポイント

この日本語訳で確認すべき点（第1章と同じく、**丁寧さ・わかりやすさ・実践できる**を基準に）：

1. **目的の明確さ：** 「何を作るか・何のための入力か」が最初から伝わるか？
2. **実用性：** 毎日やる簡単だけど少し面倒な「今日やったこと」メモが、**実際に続けやすく**なると思えるか？
3. **ストーリーの流れ：** ゴール設定 → プロジェクト作成 → コード記述 → 実行・検証 → Tabの活用、の流れが自然か？
4. **コード例・詳細説明：** そのまま動かして再現でき、**ターミナルの開き方から実行まで**、読者が**そのまま手順を辿って**試せるか？
5. **UI操作の具体性：** 「File」→「Open Folder」→「フォルダ選択」→「Open」など、**どこをクリックするか**が第1章レベルで具体的か？
6. **拡張のしやすさ：** 「次に試せること」（その日の行だけ抽出、最後の5行表示など）が、初心者にもイメージしやすいか？

---

**第3章の確認用日本語訳 - 完了**
