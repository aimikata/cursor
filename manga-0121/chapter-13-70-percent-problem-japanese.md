# 第13章：70%問題：AI生成コードを完成させる（確認用日本語訳）

> **注意：** これは確認用の日本語訳です。最終出版は英語版です。
> ストーリーの流れと内容の正確性を確認するために使用してください。

## 📖 章の概要

**章タイトル：** 70%問題：AI生成コードを完成させる  
**ページ数：** 20-22ページ（視覚学習に最適化）  
**学習目標：**
- 「AIが生成するコードは70%くらいしか完成していない」「残りの30%を完成させる必要はあるの？」という問題を理解する
- **なぜ70%問題を解決する必要か**を理解する（コードが動いてても、エラーハンドリングがないと予期しないエラーで止まる、セキュリティが不十分だと危険、など）
- AIが生成するコードに**何が足りないか**を理解する（エラーハンドリング、セキュリティ、パフォーマンス、など）
- Cursorを使って、残りの30%を完成させる方法を学ぶ

---

## 🎬 ストーリー構造

### **オープニングシーン（ページ1-2）：問題は何か？**

**【マンガパート】**

**設定：** アレックスのアパート。`daily_log`プロジェクトを開き、Cursorの**Composer**で新しい機能を追加した。コードは動いているが、何か不完全な気がする。

**マンガシーン1（ページ1）：**
- **パネル1：** アレックスがCursorの**Composer**で「`log.py`に、存在しないファイルを読み込もうとしたときのエラーハンドリングを追加して」と指示する。Composerがコードを生成する。
- **アレックス（思考）：** 「コードは動いてるけど、**エラーハンドリングが足りない**気がする。存在しないファイルを読み込もうとしたとき、エラーで止まってしまうかもしれない…。」

**パネル2：** アレックスがターミナルで`python log.py "テスト"`を実行して、コードが動くことを確認する。
- **アレックス（思考）：** 「今は動いてるけど、**予期しないエラーが起きたとき**、プログラムが止まってしまうかもしれない。でも、**コードは動いてるから、残りの30%を完成させる必要はないのでは？**」

**パネル3：** デイビッドがビデオ通話で現れる。
- **デイビッド：** 「それ、**70%問題**って言うんだ。AIが生成するコードは、**70%くらいしか完成していない**ことがある。残りの30%は、エラーハンドリング、セキュリティ、パフォーマンス、など。Cursorの**Chat**や**Composer**を使って、残りの30%を完成させよう。」
- **アレックス：** 「70%問題って、どういうことですか？それに、**コードは動いてるから、残りの30%を完成させる必要はないのでは？**」

**【インフォグラフィックパート】**

**ページ1-2、下部：**
- **この章で学ぶこと：** 70%問題の理解、AI生成コードに何が足りないかを理解する、残りの30%を完成させる方法
- **問題：** AIが生成するコードは70%くらいしか完成していない、**コードは動いてるから残りの30%を完成させる必要はないのでは？**
- **解決：** **なぜ70%問題を解決する必要か**を理解する → AIが生成するコードに**何が足りないか**を理解する → Cursorを使って残りの30%を完成させる
- **メリット：** (1) **エラーが起きても安全**（エラーハンドリングがあるから、予期しないエラーで止まらない）、(2) **セキュリティが向上する**（セキュリティ対策があるから、攻撃されにくい）、(3) **パフォーマンスが良くなる**（最適化されているから、速く動く）、(4) **本番環境で使えるコードになる**（エラーハンドリング、セキュリティ、パフォーマンスが整っている）
- **使うもの：** Cursorの**Chat**（`⌘+L`／`Ctrl+L`）、**Composer**（`⌘+I`／`Ctrl+I`）、コードレビューチェックリスト

---

### **セクション13.1：問題ー「70%問題って、どういうこと？」「残りの30%を完成させる必要はあるの？」（ページ3-4）**

**【マンガパート】**

**マンガシーン2（ページ3）：**
- **パネル1：** デイビッドが「まず、**70%問題**を理解しよう」と説明する。
- **デイビッド：** 「AIが生成するコードは、**基本的な機能は動く**けど、**エラーハンドリング、セキュリティ、パフォーマンス**などが足りないことがある。これを**70%問題**って言うんだ。残りの30%を完成させないと、**本番環境で使えない**コードになってしまうよ。」

**パネル2：** アレックスが「でも、**コードは動いてるから、残りの30%を完成させる必要はないのでは？**」と聞く。
- **デイビッド：** 「コードは動いてるけど、**予期しないエラーが起きたとき**、プログラムが止まってしまう。それに、**セキュリティが不十分**だと、攻撃されやすくなる。**パフォーマンスが悪い**と、ユーザーが使えない。残りの30%を完成させないと、**本番環境で使えない**コードになってしまうんだ。」

**パネル3：** アレックスが「70%問題を解決しないと、どうなるんですか？」と聞く。
- **デイビッド：** 「70%問題を解決しないと、(1) **エラーが起きたときにプログラムが止まる**（エラーハンドリングがないから）、(2) **セキュリティホールができる**（セキュリティ対策がないから）、(3) **パフォーマンスが悪い**（最適化されていないから）、(4) **本番環境で使えない**（エラーハンドリング、セキュリティ、パフォーマンスが整っていないから）、って問題があるよ。」

**【インフォグラフィックパート】**

**ページ3-4：**
- **問題の整理：**
  - AIが生成するコードは70%くらいしか完成していない
  - **コードは動いてるから、残りの30%を完成させる必要はないのでは？**
  - 70%問題を解決しないとどうなるかわからない
- **解決の考え方：** **なぜ70%問題を解決する必要か**を理解する → AIが生成するコードに**何が足りないか**を理解する → Cursorを使って残りの30%を完成させる
- **70%問題を解決しないとどうなるか：**
  - **エラーが起きたときにプログラムが止まる**（エラーハンドリングがないから）
  - **セキュリティホールができる**（セキュリティ対策がないから）
  - **パフォーマンスが悪い**（最適化されていないから）
  - **本番環境で使えない**（エラーハンドリング、セキュリティ、パフォーマンスが整っていないから）
- **70%問題を解決するとどうなるか：**
  - **エラーが起きても安全**（エラーハンドリングがあるから、予期しないエラーで止まらない）
  - **セキュリティが向上する**（セキュリティ対策があるから、攻撃されにくい）
  - **パフォーマンスが良くなる**（最適化されているから、速く動く）
  - **本番環境で使えるコードになる**（エラーハンドリング、セキュリティ、パフォーマンスが整っている）

---

### **セクション13.2：なぜ70%問題を解決する必要か（ページ5-8）**

**【マンガパート】**

**マンガシーン3（ページ5）：**
- **パネル1：** デイビッドが「**70%問題を解決しないとどうなるか**を具体的に見てみよう」と説明する。
- **デイビッド：** 「例えば、`log.py`で存在しないファイルを読み込もうとしたとき、**エラーハンドリングがない**と、プログラムが止まってしまう。エラーハンドリングを追加すれば、**エラーが起きても安全**に処理できるんだ。」

**パネル2：** アレックスが「でも、**コードは動いてるから、残りの30%を完成させる必要はないのでは？**」と聞く。
- **デイビッド：** 「コードは動いてるけど、**予期しないエラーが起きたとき**、プログラムが止まってしまう。それに、**セキュリティが不十分**だと、攻撃されやすくなる。**パフォーマンスが悪い**と、ユーザーが使えない。残りの30%を完成させないと、**本番環境で使えない**コードになってしまうよ。」

**パネル3：** アレックスが「70%問題を解決すると、どんなメリットがあるんですか？」と聞く。
- **デイビッド：** 「70%問題を解決すると、(1) **エラーが起きても安全**（エラーハンドリングがあるから、予期しないエラーで止まらない）、(2) **セキュリティが向上する**（セキュリティ対策があるから、攻撃されにくい）、(3) **パフォーマンスが良くなる**（最適化されているから、速く動く）、(4) **本番環境で使えるコードになる**（エラーハンドリング、セキュリティ、パフォーマンスが整っている）、ってメリットがあるよ。」

**【インフォグラフィックパート】**

**ページ5-8：**
- **70%問題を解決しないとどうなるか：**
  | 状況 | 問題 | 結果 |
  |------|------|------|
  | **エラーが起きたとき** | エラーハンドリングがない | **プログラムが止まる** |
  | **セキュリティが不十分なとき** | セキュリティ対策がない | **攻撃されやすくなる** |
  | **パフォーマンスが悪いとき** | 最適化されていない | **ユーザーが使えない** |
  | **本番環境で使うとき** | エラーハンドリング、セキュリティ、パフォーマンスが整っていない | **本番環境で使えない** |

- **70%問題を解決するとどうなるか：**
  | 状況 | メリット | 結果 |
  |------|----------|------|
  | **エラーが起きたとき** | エラーハンドリングがある | **エラーが起きても安全** |
  | **セキュリティが不十分なとき** | セキュリティ対策がある | **セキュリティが向上する** |
  | **パフォーマンスが悪いとき** | 最適化されている | **パフォーマンスが良くなる** |
  | **本番環境で使うとき** | エラーハンドリング、セキュリティ、パフォーマンスが整っている | **本番環境で使えるコードになる** |

- **70%問題を解決するメリット：**
  1. **エラーが起きても安全**（エラーハンドリングがあるから、予期しないエラーで止まらない）
  2. **セキュリティが向上する**（セキュリティ対策があるから、攻撃されにくい）
  3. **パフォーマンスが良くなる**（最適化されているから、速く動く）
  4. **本番環境で使えるコードになる**（エラーハンドリング、セキュリティ、パフォーマンスが整っている）

---

### **セクション13.3：何が足りないか：エラーハンドリング、セキュリティ、パフォーマンス（ページ9-14）**

**【マンガパート】**

**マンガシーン4（ページ9）：**
- **パネル1：** デイビッドが「AIが生成するコードに**何が足りないか**を理解しよう」と説明する。
- **デイビッド：** 「AIが生成するコードには、**エラーハンドリング、セキュリティ、パフォーマンス**が足りないことがある。例えば、`log.py`で存在しないファイルを読み込もうとしたとき、**エラーハンドリングがない**と、プログラムが止まってしまう。エラーハンドリングを追加すれば、**エラーが起きても安全**に処理できるんだ。」

**パネル2：** アレックスが「エラーハンドリング、セキュリティ、パフォーマンスって、具体的にはどんなことですか？」と聞く。
- **デイビッド：** 「(1) **エラーハンドリング**：存在しないファイルを読み込もうとしたとき、エラーが起きても安全に処理する（例：`try-except`ブロックを使う）、(2) **セキュリティ**：ユーザー入力を受け取るとき、悪意のある入力を防ぐ（例：入力検証を行う）、(3) **パフォーマンス**：大量のデータを処理するとき、効率的に処理する（例：ループを最適化する）、って感じだよ。」

**パネル3：** アレックスが「`daily_log`で実際に、何が足りないか確認してみたい」と身を乗り出す。
- **デイビッド：** 「じゃあ、`log.py`を見て、**エラーハンドリング、セキュリティ、パフォーマンス**が足りているか確認してみよう。Cursorの**Chat**に『`log.py`のコードをレビューして、エラーハンドリング、セキュリティ、パフォーマンスが足りているか確認してください』って聞けば、何が足りないか教えてくれるよ。」

**【インフォグラフィックパート】**

**ページ9-10：**
- **AIが生成するコードに足りないもの：**
  1. **エラーハンドリング**：存在しないファイルを読み込もうとしたとき、エラーが起きても安全に処理する
  2. **セキュリティ**：ユーザー入力を受け取るとき、悪意のある入力を防ぐ
  3. **パフォーマンス**：大量のデータを処理するとき、効率的に処理する

- **エラーハンドリングの例：**
  | 状況 | 問題 | 解決方法 |
  |------|------|----------|
  | **存在しないファイルを読み込む** | エラーで止まる | **`try-except`ブロックを使う** |
  | **不正な入力を処理する** | エラーで止まる | **入力検証を行う** |
  | **ネットワークエラーが起きる** | エラーで止まる | **エラーハンドリングを追加する** |

**ページ11-12：**
- **セキュリティの例：**
  | 状況 | 問題 | 解決方法 |
  |------|------|----------|
  | **ユーザー入力を受け取る** | 悪意のある入力を受け取る可能性がある | **入力検証を行う** |
  | **ファイルパスを受け取る** | 不正なパスを受け取る可能性がある | **パス検証を行う** |
  | **外部APIを呼び出す** | 不正なリクエストを送る可能性がある | **リクエスト検証を行う** |

**ページ13-14：**
- **パフォーマンスの例：**
  | 状況 | 問題 | 解決方法 |
  |------|------|----------|
  | **大量のデータを処理する** | 処理が遅い | **ループを最適化する** |
  | **ファイルを読み込む** | 読み込みが遅い | **バッファリングを使う** |
  | **データベースにアクセスする** | アクセスが遅い | **インデックスを使う** |

---

### **セクション13.4：Cursorを使って残りの30%を完成させる（ページ15-20）**

**【マンガパート】**

**マンガシーン5（ページ15）：**
- **パネル1：** デイビッドが「じゃあ、Cursorを使って、残りの30%を完成させよう」と説明する。
- **デイビッド：** 「Cursorの**Chat**に『`log.py`のコードをレビューして、エラーハンドリング、セキュリティ、パフォーマンスが足りているか確認してください。足りていない場合は、追加してください』って聞けば、何が足りないか教えてくれるし、追加してくれるよ。」

**パネル2：** アレックスが「コードレビューチェックリストって、どう使うんですか？」と聞く。
- **デイビッド：** 「コードレビューチェックリストは、**エラーハンドリング、セキュリティ、パフォーマンス**が足りているか確認するためのリストだよ。Cursorの**Chat**に『コードレビューチェックリストを使って、`log.py`をレビューしてください』って聞けば、チェックリストに基づいてレビューしてくれるよ。」

**パネル3：** アレックスが「`daily_log`で実際に、残りの30%を完成させてみたい」と身を乗り出す。
- **デイビッド：** 「じゃあ、Cursorの**Chat**で『`log.py`のコードをレビューして、エラーハンドリング、セキュリティ、パフォーマンスが足りているか確認してください』って聞いてみよう。その後、**Composer**で足りない部分を追加する流れを体験してみて。」

**【インフォグラフィックパート】**

**ページ15-16：**
- **Cursorを使って残りの30%を完成させる流れ：**
  1. **Chatでコードレビュー：** Chat（`⌘+L`／`Ctrl+L`）を開き、**「`log.py`のコードをレビューして、エラーハンドリング、セキュリティ、パフォーマンスが足りているか確認してください。足りていない場合は、追加してください」**と質問する。**Send**ボタンをクリックする
  2. **Cursorの提案を確認：** Cursorがコードレビューの結果を表示する。**必ず目で確認**して、**何が足りないか**を理解する
  3. **Composerで追加：** Composer（`⌘+I`／`Ctrl+I`）を開き、**「`log.py`にエラーハンドリング、セキュリティ、パフォーマンスを追加してください」**と指示する。**Generate**ボタンをクリックする
  4. **生成されたコードを確認：** Composerパネルに表示されたコードを**必ず目で確認**する。**何が追加されたか**を理解する

**ページ17-18：**
- **実践：Chatでコードレビューを相談する（ページ17-18）**

**【マンガパート】**

**マンガシーン6（ページ17）：**
- **パネル1：** アレックスがCursorの**Chat**（`⌘+L`／`Ctrl+L`）を開き、**「`log.py`のコードをレビューして、エラーハンドリング、セキュリティ、パフォーマンスが足りているか確認してください。足りていない場合は、追加してください」**と入力する。
- **アレックス（入力）：** 「`log.py`のコードをレビューして、エラーハンドリング、セキュリティ、パフォーマンスが足りているか確認してください。足りていない場合は、追加してください」

**パネル2：** Cursorがコードレビューの結果を表示する。
- **Cursor（応答）：** 「`log.py`のコードをレビューしました。以下の点が足りていません：
  1. **エラーハンドリング**：存在しないファイルを読み込もうとしたとき、エラーハンドリングがない
  2. **セキュリティ**：ユーザー入力を受け取るとき、入力検証がない
  3. **パフォーマンス**：大量のデータを処理するとき、最適化されていない
  
  以下のように修正してください：
  ```python
  # エラーハンドリングを追加
  try:
      with open(log_file, "a", encoding="utf-8") as f:
          f.write(f"{now:%Y-%m-%d %H:%M} {line}\n")
  except IOError as e:
      print(f"Error: {e}")
      return
  
  # 入力検証を追加
  if not line or len(line.strip()) == 0:
      print("Error: Empty message")
      return
  ```」

**パネル3：** アレックスが「コードレビューの結果が表示されたけど、**何が足りないか**がわからない…」と困る。
- **デイビッド：** 「大丈夫。Cursorの**Chat**に**『このコードレビューの結果は、どういう意味ですか？』**って聞けば、わかりやすく説明してくれるよ。」

**【インフォグラフィックパート】**

**ページ17-18：**
- **Chatでコードレビューを相談する手順：**
  1. **Chatを開く：** Chat（`⌘+L`／`Ctrl+L`）を開く
  2. **質問を入力：** **「`log.py`のコードをレビューして、エラーハンドリング、セキュリティ、パフォーマンスが足りているか確認してください。足りていない場合は、追加してください」**と入力する
  3. **Sendボタンをクリック：** **Send**ボタンをクリックする
  4. **Cursorの提案を確認：** Cursorがコードレビューの結果を表示する。**必ず目で確認**して、**何が足りないか**を理解する
  5. **コードレビュー結果の説明を聞く：** 必要に応じて、Cursorの**Chat**に**「このコードレビューの結果は、どういう意味ですか？」**と質問する

**ページ19-20：**
- **実践：Composerで残りの30%を完成させる（ページ19-20）**

**【マンガパート】**

**マンガシーン7（ページ19）：**
- **パネル1：** アレックスがCursorの**Composer**（`⌘+I`／`Ctrl+I`）を開き、**「`log.py`にエラーハンドリング、セキュリティ、パフォーマンスを追加してください」**と指示する。
- **アレックス（入力）：** 「`log.py`にエラーハンドリング、セキュリティ、パフォーマンスを追加してください」

**パネル2：** Composerがコードを生成する。
- **Composer（提案）：** `log.py`に以下のコードを追加する：
  ```python
  # エラーハンドリングを追加
  try:
      with open(log_file, "a", encoding="utf-8") as f:
          f.write(f"{now:%Y-%m-%d %H:%M} {line}\n")
  except IOError as e:
      print(f"Error: {e}")
      return
  
  # 入力検証を追加
  if not line or len(line.strip()) == 0:
      print("Error: Empty message")
      return
  ```

**パネル3：** アレックスが「生成されたコードを確認したい」と言う。
- **デイビッド：** 「Composerパネルに表示されたコードを**必ず目で確認**して、**何が追加されたか**を理解しよう。その後、**Accept**ボタンをクリックして、コードを追加するよ。」

**【インフォグラフィックパート】**

**ページ19-20：**
- **Composerで残りの30%を完成させる手順：**
  1. **Composerを開く：** Composer（`⌘+I`／`Ctrl+I`）を開く
  2. **指示を入力：** **「`log.py`にエラーハンドリング、セキュリティ、パフォーマンスを追加してください」**と指示する
  3. **Generateボタンをクリック：** **Generate**ボタンをクリックする
  4. **生成されたコードを確認：** Composerパネルに表示されたコードを**必ず目で確認**する。**何が追加されたか**を理解する
  5. **Acceptボタンをクリック：** **Accept**ボタンをクリックして、コードを追加する

- **生成されたコードの確認ポイント：**
  - **エラーハンドリングが追加されたか**（`try-except`ブロックなど）
  - **セキュリティ対策が追加されたか**（入力検証など）
  - **パフォーマンスが改善されたか**（最適化など）

---

## 📝 章のまとめ（ページ21-22）

**【マンガパート】**

**マンガシーン8（ページ21）：**
- **パネル1：** デイビッドが「この章では、(1) **70%問題**を理解した（AIが生成するコードは70%くらいしか完成していない）、(2) **なぜ70%問題を解決する必要か**を理解した（コードが動いてても、エラーハンドリングがないと予期しないエラーで止まる、セキュリティが不十分だと危険、など）、(3) AIが生成するコードに**何が足りないか**を理解した（エラーハンドリング、セキュリティ、パフォーマンス、など）、(4) Cursorを使って、残りの30%を完成させる方法を学んだ、ってところだね。」
- **アレックス：** 「70%問題を解決すると、**エラーが起きても安全**で、**セキュリティが向上**して、**パフォーマンスが良くなる**んですね。**本番環境で使えるコード**になるんですね。」

**パネル2：** アレックスが「でも、**コードは動いてるから、残りの30%を完成させる必要はないのでは？**」と確認する。
- **デイビッド：** 「コードは動いてるけど、**予期しないエラーが起きたとき**、プログラムが止まってしまう。それに、**セキュリティが不十分**だと、攻撃されやすくなる。**パフォーマンスが悪い**と、ユーザーが使えない。残りの30%を完成させないと、**本番環境で使えない**コードになってしまうよ。」

**パネル3：** アレックスが「次は、どんなことを学ぶんですか？」と聞く。
- **デイビッド：** 「次は、**実践プロジェクト**について学ぼう。これまで学んだことを使って、実際のプロジェクトを作ってみよう。」

**【インフォグラフィックパート】**

**ページ21-22：**
- **この章で学んだこと：**
  - ✅ **70%問題**を理解した：AIが生成するコードは70%くらいしか完成していない
  - ✅ **なぜ70%問題を解決する必要か**を理解した：コードが動いてても、エラーハンドリングがないと予期しないエラーで止まる、セキュリティが不十分だと危険、など
  - ✅ AIが生成するコードに**何が足りないか**を理解した：エラーハンドリング、セキュリティ、パフォーマンス、など
  - ✅ Cursorを使って、残りの30%を完成させる方法を学んだ
  - ✅ **70%問題を解決しないとどうなるか**を理解した：エラーが起きたときにプログラムが止まる、セキュリティホールができる、パフォーマンスが悪い、本番環境で使えない、など
  - ✅ **70%問題を解決するとどうなるか**を理解した：エラーが起きても安全、セキュリティが向上する、パフォーマンスが良くなる、本番環境で使えるコードになる、など

- **70%問題を解決するメリット：**
  1. **エラーが起きても安全**（エラーハンドリングがあるから、予期しないエラーで止まらない）
  2. **セキュリティが向上する**（セキュリティ対策があるから、攻撃されにくい）
  3. **パフォーマンスが良くなる**（最適化されているから、速く動く）
  4. **本番環境で使えるコードになる**（エラーハンドリング、セキュリティ、パフォーマンスが整っている）

- **コードレビューチェックリスト：**
  - ✅ **エラーハンドリング**：存在しないファイルを読み込もうとしたとき、エラーが起きても安全に処理する
  - ✅ **セキュリティ**：ユーザー入力を受け取るとき、悪意のある入力を防ぐ
  - ✅ **パフォーマンス**：大量のデータを処理するとき、効率的に処理する

- **次の章：** 第14章以降「実践プロジェクト」

---

**確認完了日：** 2025-01-21
