# 第15章：プロジェクト2：API連携アプリを作る（確認用日本語訳）

> **注意：** これは確認用の日本語訳です。最終出版は英語版です。
> ストーリーの流れと内容の正確性を確認するために使用してください。

## 📖 章の概要

**章タイトル：** プロジェクト2：API連携アプリを作る  
**ページ数：** 24-26ページ（視覚学習に最適化）  
**学習目標：**
- 外部APIと連携する方法を学ぶ
- API呼び出しの**エラーハンドリング**と**エッジケース**を処理する方法を学ぶ
- API呼び出しの**テスト**を書く方法を学ぶ
- API連携の**ベストプラクティス**を学ぶ

---

## 🎬 ストーリー構造

### **オープニングシーン（ページ1-2）：何を作る？**

**【マンガパート】**

**設定：** アレックスのアパート。第14章でToDoアプリを作った後、もっと実用的なアプリを作りたいと思っている。

**マンガシーン1（ページ1）：**
- **パネル1：** アレックスが「ToDoアプリは完成したけど、**もっと実用的なアプリ**を作りたいな。でも、**何を作ればいいか**わからない…。」
- **アレックス（思考）：** 「天気情報を取得するアプリとか、**外部のAPIと連携**するアプリを作ってみたい。でも、**APIって何？どうやって使うの？**」

**パネル2：** デイビッドがビデオ通話で現れる。
- **デイビッド：** 「それ、**API連携アプリ**を作ってみよう。APIは、**外部のサービスと連携**するための仕組み。例えば、天気情報を取得するAPI、ニュースを取得するAPI、など。Cursorの**Chat**や**Composer**を使って、API連携アプリを作れるよ。」
- **アレックス：** 「API連携アプリって、どうやって作るんですか？**APIって何？どうやって使うの？**」

**パネル3：** デイビッドが「**ステップバイステップ**で一緒に作っていこう」と提案する。
- **デイビッド：** 「まず、**APIとは何か**を理解して、その後、Cursorの**Chat**や**Composer**を使って、**ステップバイステップ**でAPI連携アプリを作っていこう。**エラーハンドリング**や**テスト**も一緒に学んでいくよ。」

**【インフォグラフィックパート】**

**ページ1-2、下部：**
- **この章で作るもの：** API連携アプリ（天気情報を取得するアプリ）
- **この章で学ぶこと：** APIとは何か、外部APIと連携する方法、**エラーハンドリング**と**エッジケース**の処理、API呼び出しの**テスト**、API連携の**ベストプラクティス**
- **使うもの：** Cursorの**Chat**（`⌘+L`／`Ctrl+L`）、**Composer**（`⌘+I`／`Ctrl+I`）、Pythonの`requests`ライブラリ、API（例：OpenWeatherMap API）
- **流れ：** 15.1でAPIとは何かを理解 → 15.2でエラーハンドリングとエッジケースを処理 → 15.3でAPI呼び出しのテストを書く → 15.4でAPI連携のベストプラクティスを学ぶ

---

### **セクション15.1：外部APIと連携する（ページ3-10）**

**【マンガパート】**

**マンガシーン2（ページ3）：**
- **パネル1：** デイビッドが「まず、**APIとは何か**を理解しよう」と説明する。
- **デイビッド：** 「APIは、**外部のサービスと連携**するための仕組み。例えば、天気情報を取得するAPI、ニュースを取得するAPI、など。APIを使うと、**自分でデータを用意しなくても**、外部のサービスからデータを取得できるんだ。」

**パネル2：** アレックスが「**APIって何？どうやって使うの？**」と聞く。
- **デイビッド：** 「APIは、**HTTPリクエスト**を送って、**JSONデータ**を受け取る仕組み。Pythonの`requests`ライブラリを使えば、簡単にAPIを呼び出せるよ。Cursorの**Chat**や**Composer**に『天気情報を取得するAPI連携アプリを作ってください』って指示すれば、API連携アプリを作ってくれるよ。」

**パネル3：** アレックスが「`daily_log`で実際に、API連携アプリを作ってみたい」と身を乗り出す。
- **デイビッド：** 「じゃあ、Cursorの**Chat**で『天気情報を取得するAPI連携アプリを作りたいです。OpenWeatherMap APIを使います。プロジェクトの構造を提案してください』って聞いてみよう。その後、**Composer**でAPI連携アプリを作成する流れを体験してみて。」

**【インフォグラフィックパート】**

**ページ3-4：**
- **APIとは何か：**
  1. **API（Application Programming Interface）**：外部のサービスと連携するための仕組み
  2. **HTTPリクエスト**：APIにリクエストを送る（例：`GET https://api.example.com/data`）
  3. **JSONデータ**：APIからデータを受け取る（例：`{"temperature": 25, "humidity": 60}`）

- **APIを使うメリット：**
  - **自分でデータを用意しなくても**、外部のサービスからデータを取得できる
  - **最新のデータ**を取得できる
  - **複雑な処理**を外部のサービスに任せられる

**ページ5-6：**
- **実践：ChatでAPI連携アプリの計画を相談する（ページ5-6）**

**【マンガパート】**

**マンガシーン3（ページ5）：**
- **パネル1：** アレックスがCursorの**Chat**（`⌘+L`／`Ctrl+L`）を開き、**「天気情報を取得するAPI連携アプリを作りたいです。OpenWeatherMap APIを使います。プロジェクトの構造を提案してください」**と入力する。
- **アレックス（入力）：** 「天気情報を取得するAPI連携アプリを作りたいです。OpenWeatherMap APIを使います。プロジェクトの構造を提案してください」

**パネル2：** Cursorがプロジェクトの構造を提案する。
- **Cursor（応答）：** 「天気情報を取得するAPI連携アプリのプロジェクト構造を提案します：
  ```
  weather_app/
  ├── weather.py       # メインのコード
  ├── config.py        # APIキーの設定
  └── test_weather.py  # テストコード
  ```
  
  `weather.py`には以下の機能を実装します：
  - `get_weather(city)`：指定した都市の天気情報を取得する
  - `main()`：メイン処理
  ```」

**パネル3：** アレックスが「プロジェクトの構造が提案されたけど、**APIって何？どうやって使うの？**」と困る。
- **デイビッド：** 「大丈夫。Cursorの**Composer**で『`weather_app`フォルダを作成して、`weather.py`ファイルを作成してください。OpenWeatherMap APIを使って、指定した都市の天気情報を取得する機能を実装してください』って指示すれば、API連携アプリを作成してくれるよ。」

**【インフォグラフィックパート】**

**ページ5-6：**
- **ChatでAPI連携アプリの計画を相談する手順：**
  1. **Chatを開く：** Chat（`⌘+L`／`Ctrl+L`）を開く
  2. **質問を入力：** **「天気情報を取得するAPI連携アプリを作りたいです。OpenWeatherMap APIを使います。プロジェクトの構造を提案してください」**と入力する
  3. **Sendボタンをクリック：** **Send**ボタンをクリックする
  4. **Cursorの提案を確認：** Cursorがプロジェクトの構造を提案する。**必ず目で確認**して、**どんな構造か**を理解する

**ページ7-8：**
- **実践：ComposerでAPI連携アプリを作成する（ページ7-8）**

**【マンガパート】**

**マンガシーン4（ページ7）：**
- **パネル1：** アレックスがCursorの**Composer**（`⌘+I`／`Ctrl+I`）を開き、**「`weather_app`フォルダを作成して、`weather.py`ファイルを作成してください。OpenWeatherMap APIを使って、指定した都市の天気情報を取得する機能を実装してください」**と指示する。
- **アレックス（入力）：** 「`weather_app`フォルダを作成して、`weather.py`ファイルを作成してください。OpenWeatherMap APIを使って、指定した都市の天気情報を取得する機能を実装してください」

**パネル2：** ComposerがAPI連携アプリを作成する。
- **Composer（提案）：** `weather_app`フォルダを作成し、`weather.py`ファイルを作成する：
  ```python
  # weather.py - 天気情報を取得するAPI連携アプリ
  import requests
  import json
  import os
  
  API_KEY = os.getenv("OPENWEATHER_API_KEY", "your_api_key_here")
  BASE_URL = "https://api.openweathermap.org/data/2.5/weather"
  
  def get_weather(city):
      """指定した都市の天気情報を取得する"""
      params = {
          "q": city,
          "appid": API_KEY,
          "units": "metric",
          "lang": "ja"
      }
      response = requests.get(BASE_URL, params=params)
      if response.status_code == 200:
          data = response.json()
          return {
              "city": data["name"],
              "temperature": data["main"]["temp"],
              "description": data["weather"][0]["description"],
              "humidity": data["main"]["humidity"]
          }
      else:
          return None
  
  def main():
      """メイン処理"""
      import sys
      if len(sys.argv) < 2:
          print("Usage: python weather.py \"都市名\"")
          return
      city = sys.argv[1]
      weather = get_weather(city)
      if weather:
          print(f"都市: {weather['city']}")
          print(f"気温: {weather['temperature']}°C")
          print(f"天気: {weather['description']}")
          print(f"湿度: {weather['humidity']}%")
      else:
          print("天気情報を取得できませんでした")
  
  if __name__ == "__main__":
      main()
  ```

**パネル3：** アレックスが「生成されたコードを確認したい」と言う。
- **デイビッド：** 「Composerパネルに表示されたコードを**必ず目で確認**して、**どんな機能が実装されているか**を理解しよう。その後、**Accept**ボタンをクリックして、API連携アプリを作成するよ。」

**【インフォグラフィックパート】**

**ページ7-8：**
- **ComposerでAPI連携アプリを作成する手順：**
  1. **Composerを開く：** Composer（`⌘+I`／`Ctrl+I`）を開く
  2. **指示を入力：** **「`weather_app`フォルダを作成して、`weather.py`ファイルを作成してください。OpenWeatherMap APIを使って、指定した都市の天気情報を取得する機能を実装してください」**と指示する
  3. **Generateボタンをクリック：** **Generate**ボタンをクリックする
  4. **生成されたコードを確認：** Composerパネルに表示されたコードを**必ず目で確認**する。**どんな機能が実装されているか**を理解する
  5. **Acceptボタンをクリック：** **Accept**ボタンをクリックして、API連携アプリを作成する

**ページ9-10：**
- **API連携アプリの使い方：**
  ```bash
  # 環境変数にAPIキーを設定
  export OPENWEATHER_API_KEY="your_api_key_here"
  
  # 天気情報を取得
  python weather.py "Tokyo"
  ```

---

### **セクション15.2：エラーハンドリングとエッジケース（ページ11-16）**

**【マンガパート】**

**マンガシーン5（ページ11）：**
- **パネル1：** アレックスがターミナルで`python weather.py "Tokyo"`を実行する。エラーが出る。
- **エラーメッセージ（例）：**
  ```
  Traceback (most recent call last):
    File "weather.py", line 25, in get_weather
      data = response.json()
  requests.exceptions.JSONDecodeError: Expecting value: line 1 column 1 (char 0)
  ```
- **アレックス：** 「エラーが出た…。**どうすればいいか**わからない…。」

**パネル2：** デイビッドが「API呼び出しでは、**エラーハンドリング**が大事だよ」と説明する。
- **デイビッド：** 「API呼び出しでは、**ネットワークエラー**、**APIエラー**、**データが存在しない**、など、いろいろなエラーが起きる可能性がある。**エラーハンドリング**を追加すれば、**エラーが起きても安全**に処理できるんだ。」

**パネル3：** アレックスが「**エラーハンドリング**って、どうやって追加すればいいんですか？」と聞く。
- **デイビッド：** 「Cursorの**Chat**や**Composer**に『`weather.py`に、API呼び出しのエラーハンドリングを追加してください。ネットワークエラー、APIエラー、データが存在しない場合の処理を追加してください』って指示すれば、エラーハンドリングを追加してくれるよ。」

**【インフォグラフィックパート】**

**ページ11-12：**
- **API呼び出しで起きるエラー：**
  | エラー | 原因 | 解決方法 |
  |--------|------|----------|
  | **ネットワークエラー** | インターネット接続がない | **`try-except`ブロック**でエラーをキャッチする |
  | **APIエラー** | APIキーが無効、リクエストが不正 | **`response.status_code`を確認**する |
  | **データが存在しない** | 指定した都市が存在しない | **`response.json()`の結果を確認**する |

**ページ13-14：**
- **実践：Chatでエラーハンドリングを相談する（ページ13-14）**

**【マンガパート】**

**マンガシーン6（ページ13）：**
- **パネル1：** アレックスがCursorの**Chat**（`⌘+L`／`Ctrl+L`）を開き、**「`weather.py`に、API呼び出しのエラーハンドリングを追加してください。ネットワークエラー、APIエラー、データが存在しない場合の処理を追加してください」**と入力する。
- **アレックス（入力）：** 「`weather.py`に、API呼び出しのエラーハンドリングを追加してください。ネットワークエラー、APIエラー、データが存在しない場合の処理を追加してください」

**パネル2：** Cursorがエラーハンドリングの提案を表示する。
- **Cursor（応答）：** 「`weather.py`に以下のエラーハンドリングを追加します：
  ```python
  def get_weather(city):
      """指定した都市の天気情報を取得する"""
      try:
          params = {
              "q": city,
              "appid": API_KEY,
              "units": "metric",
              "lang": "ja"
          }
          response = requests.get(BASE_URL, params=params, timeout=10)
          
          if response.status_code == 200:
              data = response.json()
              return {
                  "city": data["name"],
                  "temperature": data["main"]["temp"],
                  "description": data["weather"][0]["description"],
                  "humidity": data["main"]["humidity"]
              }
          elif response.status_code == 404:
              return {"error": "都市が見つかりませんでした"}
          else:
              return {"error": f"APIエラー: {response.status_code}"}
      except requests.exceptions.RequestException as e:
          return {"error": f"ネットワークエラー: {e}"}
      except json.JSONDecodeError:
          return {"error": "データの解析に失敗しました"}
  ```」

**パネル3：** アレックスが「エラーハンドリングが追加されたけど、**どんなエラーを処理しているか**がわからない…」と困る。
- **デイビッド：** 「大丈夫。Cursorの**Chat**に**『このエラーハンドリングは、どんなエラーを処理していますか？』**って聞けば、わかりやすく説明してくれるよ。」

**【インフォグラフィックパート】**

**ページ13-14：**
- **Chatでエラーハンドリングを相談する手順：**
  1. **Chatを開く：** Chat（`⌘+L`／`Ctrl+L`）を開く
  2. **質問を入力：** **「`weather.py`に、API呼び出しのエラーハンドリングを追加してください。ネットワークエラー、APIエラー、データが存在しない場合の処理を追加してください」**と入力する
  3. **Sendボタンをクリック：** **Send**ボタンをクリックする
  4. **Cursorの提案を確認：** Cursorがエラーハンドリングの提案を表示する。**必ず目で確認**して、**どんなエラーを処理しているか**を理解する

**ページ15-16：**
- **実践：Composerでエラーハンドリングを追加する（ページ15-16）**

**【マンガパート】**

**マンガシーン7（ページ15）：**
- **パネル1：** アレックスがCursorの**Composer**（`⌘+I`／`Ctrl+I`）を開き、**「`weather.py`に、API呼び出しのエラーハンドリングを追加してください。ネットワークエラー、APIエラー、データが存在しない場合の処理を追加してください」**と指示する。
- **アレックス（入力）：** 「`weather.py`に、API呼び出しのエラーハンドリングを追加してください。ネットワークエラー、APIエラー、データが存在しない場合の処理を追加してください」

**パネル2：** Composerがエラーハンドリングを追加する。
- **Composer（提案）：** `weather.py`に以下のエラーハンドリングを追加する：
  ```python
  def get_weather(city):
      """指定した都市の天気情報を取得する"""
      try:
          params = {
              "q": city,
              "appid": API_KEY,
              "units": "metric",
              "lang": "ja"
          }
          response = requests.get(BASE_URL, params=params, timeout=10)
          
          if response.status_code == 200:
              data = response.json()
              return {
                  "city": data["name"],
                  "temperature": data["main"]["temp"],
                  "description": data["weather"][0]["description"],
                  "humidity": data["main"]["humidity"]
              }
          elif response.status_code == 404:
              return {"error": "都市が見つかりませんでした"}
          else:
              return {"error": f"APIエラー: {response.status_code}"}
      except requests.exceptions.RequestException as e:
          return {"error": f"ネットワークエラー: {e}"}
      except json.JSONDecodeError:
          return {"error": "データの解析に失敗しました"}
  ```

**パネル3：** アレックスが「生成されたコードを確認したい」と言う。
- **デイビッド：** 「Composerパネルに表示されたコードを**必ず目で確認**して、**どんなエラーを処理しているか**を理解しよう。その後、**Accept**ボタンをクリックして、エラーハンドリングを追加するよ。」

**【インフォグラフィックパート】**

**ページ15-16：**
- **Composerでエラーハンドリングを追加する手順：**
  1. **Composerを開く：** Composer（`⌘+I`／`Ctrl+I`）を開く
  2. **指示を入力：** **「`weather.py`に、API呼び出しのエラーハンドリングを追加してください。ネットワークエラー、APIエラー、データが存在しない場合の処理を追加してください」**と指示する
  3. **Generateボタンをクリック：** **Generate**ボタンをクリックする
  4. **生成されたコードを確認：** Composerパネルに表示されたコードを**必ず目で確認**する。**どんなエラーを処理しているか**を理解する
  5. **Acceptボタンをクリック：** **Accept**ボタンをクリックして、エラーハンドリングを追加する

---

### **セクション15.3：API呼び出しのテストを書く（ページ17-20）**

**【マンガパート】**

**マンガシーン8（ページ17）：**
- **パネル1：** デイビッドが「じゃあ、API呼び出しの**テスト**を書いてみよう」と説明する。
- **デイビッド：** 「API呼び出しのテストを書いておくと、**APIが変更されたときに、コードが壊れていないか確認**できる。Cursorの**Chat**や**Composer**に『`test_weather.py`を作成して、`weather.py`のAPI呼び出しをテストしてください』って指示すれば、テストコードを生成してくれるよ。」

**パネル2：** アレックスが「API呼び出しのテストって、どうやって書くんですか？」と聞く。
- **デイビッド：** 「API呼び出しのテストでは、**モック**を使う。モックは、**実際のAPIを呼び出さずに**、テスト用のデータを返す仕組み。Cursorが生成してくれるから、**テストの結果を理解**すればいいよ。」

**パネル3：** アレックスが「`weather_app`で実際に、API呼び出しのテストを書いてみたい」と身を乗り出す。
- **デイビッド：** 「じゃあ、Cursorの**Chat**で『`test_weather.py`を作成して、`weather.py`のAPI呼び出しをテストしてください。`pytest`と`unittest.mock`を使って、モックを使ったテストを書いてください』って聞いてみよう。その後、**Composer**でテストファイルを作成する流れを体験してみて。」

**【インフォグラフィックパート】**

**ページ17-18：**
- **API呼び出しのテストを書く手順：**
  1. **Chatで質問：** Chat（`⌘+L`／`Ctrl+L`）を開き、**「`test_weather.py`を作成して、`weather.py`のAPI呼び出しをテストしてください。`pytest`と`unittest.mock`を使って、モックを使ったテストを書いてください」**と質問する
  2. **Cursorの提案を確認：** Cursorがテストコードの提案を表示する。**必ず目で確認**して、**どんなことをテストしているか**を理解する
  3. **Composerで生成：** Composer（`⌘+I`／`Ctrl+I`）を開き、**「`test_weather.py`を作成して、`weather.py`のAPI呼び出しをテストしてください」**と指示する
  4. **テストを実行：** ターミナルで`pytest test_weather.py`を実行する

**ページ19-20：**
- **モックを使ったテストの例：**
  ```python
  import pytest
  from unittest.mock import patch, Mock
  from weather import get_weather
  
  @patch('weather.requests.get')
  def test_get_weather_success(mock_get):
      """正常なAPI呼び出しのテスト"""
      mock_response = Mock()
      mock_response.status_code = 200
      mock_response.json.return_value = {
          "name": "Tokyo",
          "main": {"temp": 25, "humidity": 60},
          "weather": [{"description": "晴れ"}]
      }
      mock_get.return_value = mock_response
      
      result = get_weather("Tokyo")
      assert result["city"] == "Tokyo"
      assert result["temperature"] == 25
  ```

---

### **セクション15.4：API連携のベストプラクティス（ページ21-24）**

**【マンガパート】**

**マンガシーン9（ページ21）：**
- **パネル1：** デイビッドが「API連携の**ベストプラクティス**を学ぼう」と説明する。
- **デイビッド：** 「API連携では、**APIキーを環境変数に保存する**、**リクエストにタイムアウトを設定する**、**エラーハンドリングを追加する**、**テストを書く**、などが大事だよ。」

**パネル2：** アレックスが「API連携の**ベストプラクティス**って、具体的にはどんなことですか？」と聞く。
- **デイビッド：** 「(1) **APIキーを環境変数に保存する**（セキュリティのため）、(2) **リクエストにタイムアウトを設定する**（ネットワークエラーを防ぐため）、(3) **エラーハンドリングを追加する**（エラーが起きても安全に処理するため）、(4) **テストを書く**（APIが変更されたときに、コードが壊れていないか確認するため）、って感じだよ。」

**パネル3：** アレックスが「`weather_app`で実際に、ベストプラクティスを適用してみたい」と身を乗り出す。
- **デイビッド：** 「じゃあ、Cursorの**Chat**で『`weather.py`に、API連携のベストプラクティスを適用してください。APIキーを環境変数に保存する、リクエストにタイムアウトを設定する、エラーハンドリングを追加する、など』って聞いてみよう。」

**【インフォグラフィックパート】**

**ページ21-22：**
- **API連携のベストプラクティス：**
  1. **APIキーを環境変数に保存する**：セキュリティのため
  2. **リクエストにタイムアウトを設定する**：ネットワークエラーを防ぐため
  3. **エラーハンドリングを追加する**：エラーが起きても安全に処理するため
  4. **テストを書く**：APIが変更されたときに、コードが壊れていないか確認するため

**ページ23-24：**
- **ベストプラクティスの適用例：**
  ```python
  # APIキーを環境変数に保存
  API_KEY = os.getenv("OPENWEATHER_API_KEY")
  
  # リクエストにタイムアウトを設定
  response = requests.get(BASE_URL, params=params, timeout=10)
  
  # エラーハンドリングを追加
  try:
      # API呼び出し
  except requests.exceptions.RequestException as e:
      # エラー処理
  ```

---

## 📝 章のまとめ（ページ25-26）

**【マンガパート】**

**マンガシーン10（ページ25）：**
- **パネル1：** デイビッドが「この章では、(1) 外部APIと連携する方法を学んだ、(2) API呼び出しの**エラーハンドリング**と**エッジケース**を処理する方法を学んだ、(3) API呼び出しの**テスト**を書く方法を学んだ、(4) API連携の**ベストプラクティス**を学んだ、ってところだね。」
- **アレックス：** 「API連携アプリが完成しました！**エラーハンドリング**や**テスト**も追加できました。」

**パネル2：** アレックスが「でも、**APIって何？どうやって使うの？**って最初はわからなかった…」と振り返る。
- **デイビッド：** 「大丈夫。**ステップバイステップ**で一緒にやっていけば、**APIとは何か**、**どうやって使うか**がわかるようになるよ。**エラーハンドリング**や**テスト**も一緒に学んでいけば、**実用的なAPI連携アプリ**を作れるようになるよ。」

**パネル3：** アレックスが「次は、どんなことを学ぶんですか？」と聞く。
- **デイビッド：** 「次は、**データベースアプリ**を作ってみよう。データベースにデータを保存・取得する方法を学ぼう。」

**【インフォグラフィックパート】**

**ページ25-26：**
- **この章で学んだこと：**
  - ✅ 外部APIと連携する方法を学んだ
  - ✅ API呼び出しの**エラーハンドリング**と**エッジケース**を処理する方法を学んだ
  - ✅ API呼び出しの**テスト**を書く方法を学んだ
  - ✅ API連携の**ベストプラクティス**を学んだ

- **API連携のベストプラクティス：**
  1. **APIキーを環境変数に保存する**：セキュリティのため
  2. **リクエストにタイムアウトを設定する**：ネットワークエラーを防ぐため
  3. **エラーハンドリングを追加する**：エラーが起きても安全に処理するため
  4. **テストを書く**：APIが変更されたときに、コードが壊れていないか確認するため

- **次の章：** 第16章「プロジェクト3：データベースアプリを作る」

---

**確認完了日：** 2025-01-21
