# 第17章：チーム開発でのCursor活用（確認用日本語訳）

> **注意：** これは確認用の日本語訳です。最終出版は英語版です。
> ストーリーの流れと内容の正確性を確認するために使用してください。

## 📖 章の概要

**章タイトル：** チーム開発でのCursor活用  
**ページ数：** 20-22ページ（視覚学習に最適化）  
**学習目標：**
- **チームで`.cursorrules`を共有する**方法を学ぶ（Gitリポジトリに含める、共有のベストプラクティス）
- **Cursorをコードレビューで活用する**方法を学ぶ（変更内容の説明、潜在的な問題の指摘、改善提案）
- **チーム開発でのベストプラクティス**を学ぶ（`.cursorrules`の更新、コードスタイルの統一、コミュニケーション）

---

## 🎬 ストーリー構造

### **オープニングシーン（ページ1-2）：チーム開発での課題**

**【マンガパート】**

**設定：** アレックスのアパート。`daily_log`プロジェクトを開き、Cursorを使っている。最近、チーム開発に参加することになった。

**マンガシーン1（ページ1）：**
- **パネル1：** アレックスがチームメンバーから「コードスタイルがバラバラだね。統一しよう」と言われる。
- **アレックス（思考）：** 「自分は`.cursorrules`を作ってるけど、**チームメンバー全員が同じルールを使える**ようにするには、どうすればいいんだろう？」

**パネル2：** アレックスがコードレビューで「この変更、何をしてるの？」と聞かれる。
- **アレックス（思考）：** 「コードレビューで、**変更内容を説明する**のが大変だな。Cursorを使って、**変更内容を自動で説明**してもらえるかな？」

**パネル3：** デイビッドがビデオ通話で現れる。
- **デイビッド：** 「チーム開発では、**`.cursorrules`を共有**して、**Cursorをコードレビューで活用**するのがおすすめだよ。Chapter 12で`.cursorrules`を作ったから、今度は**チームで共有する方法**と、**コードレビューでの使い方**を学ぼう。」
- **アレックス：** 「チーム開発でのCursorの活用法を教えてください。」

**【インフォグラフィックパート】**

**ページ1-2、下部：**
- **この章で学ぶこと：** チーム開発でのCursor活用方法
- **問題：** チーム開発で、コードスタイルがバラバラ、コードレビューで変更内容を説明するのが大変
- **解決：** **`.cursorrules`をチームで共有** → **Cursorをコードレビューで活用** → **ベストプラクティスを実践**
- **使うもの：** `.cursorrules`ファイル、Gitリポジトリ、Cursorの**Chat**（`⌘+L`／`Ctrl+L`）、**Composer**（`⌘+I`／`Ctrl+I`）

---

### **セクション17.1：チームで`.cursorrules`を共有する（ページ3-9）**

**【マンガパート】**

**マンガシーン2（ページ3）：**
- **パネル1：** デイビッドが「チーム開発では、**`.cursorrules`を共有**することで、**全員が同じルール**を使えるようになるよ」と説明する。
- **デイビッド：** 「Chapter 12で`.cursorrules`を作ったけど、**チームメンバー全員が同じルール**を使うには、**Gitリポジトリに含める**のが一番簡単だよ。」

**パネル2：** アレックスが「`.cursorrules`をGitリポジトリに含めるって、どうすればいいんですか？」と聞く。
- **デイビッド：** 「`.cursorrules`ファイルをプロジェクトの**ルートディレクトリ**に置いて、**Gitにコミット**すればいい。チームメンバーがリポジトリをクローンすると、**自動で`.cursorrules`が含まれる**から、全員が同じルールを使えるよ。」

**パネル3：** アレックスが「`daily_log`プロジェクトで、`.cursorrules`をGitリポジトリに含めてみたい」と身を乗り出す。
- **デイビッド：** 「じゃあ、`.cursorrules`ファイルをプロジェクトのルートディレクトリに置いて、Gitにコミットする流れを体験してみよう。」

**【インフォグラフィックパート】**

**ページ3-4：**
- **`.cursorrules`をチームで共有する方法：**
  1. **`.cursorrules`ファイルをプロジェクトのルートディレクトリに置く：** プロジェクトのルートディレクトリ（`daily_log`フォルダの直下）に`.cursorrules`ファイルを作成する
  2. **Gitにコミット：** `.cursorrules`ファイルをGitにコミットする（`git add .cursorrules`、`git commit -m "Add .cursorrules"`）
  3. **チームメンバーがリポジトリをクローン：** チームメンバーがリポジトリをクローンすると、**自動で`.cursorrules`が含まれる**
  4. **全員が同じルールを使う：** チームメンバー全員が同じ`.cursorrules`を使うことで、**コードスタイルが統一**される
- **ポイント：** `.cursorrules`をGitリポジトリに含めることで、**チーム全体で一貫性**が保てる。

**【マンガパート】**

**マンガシーン3-1（ページ4-5）：**
- **パネル1：** アレックスが`daily_log`プロジェクトのルートディレクトリに`.cursorrules`ファイルを作成する。
- **デイビッド：** 「`.cursorrules`ファイルをプロジェクトのルートディレクトリ（`daily_log`フォルダの直下）に置く。EXPLORERで`daily_log`を右クリック→「New File」→`.cursorrules`と入力してEnter。」
- **視覚的表現：** EXPLORERで`daily_log`を右クリックし、「New File」を選択して`.cursorrules`と入力する様子。

**パネル2：** アレックスが`.cursorrules`ファイルに、プロジェクトのルールを書く。
- **アレックス（`.cursorrules`の内容）：**
  ```
  # コードスタイル
  - 関数名は`snake_case`で書く
  - コメントは日本語で書く
  - インデントは4スペース
  
  # プロジェクトのルール
  - `log.py`は、`daily_log.txt`にログを追記するスクリプト
  - `--day`オプションで、その日の行だけ表示する
  ```
- **デイビッド：** 「Chapter 12で学んだように、**プロジェクトのルール**を`.cursorrules`ファイルに書く。チームメンバー全員が同じルールを使えるように、**わかりやすく書く**のが大事だよ。」

**パネル3：** アレックスがターミナルでGitにコミットする。
- **デイビッド：** 「ターミナルで`git add .cursorrules`と`git commit -m "Add .cursorrules for team"`を実行して、Gitにコミットする。チームメンバーがリポジトリをクローンすると、**自動で`.cursorrules`が含まれる**よ。」
- **視覚的表現：** ターミナルで`git add .cursorrules`と`git commit -m "Add .cursorrules for team"`を実行する様子。

**【テキストパート - 詳細説明】**

**ページ5-6、下部：**
- **`.cursorrules`をチームで共有する完全手順（そのまま再現できる）：**
  1. **`.cursorrules`ファイルを作成：** EXPLORERでプロジェクトのルートディレクトリ（`daily_log`フォルダ）を右クリック→「**New File**」→`.cursorrules`と入力してEnter
  2. **`.cursorrules`ファイルにルールを書く：** `.cursorrules`ファイルを開き、プロジェクトのルール（コードスタイル、コメントの書き方、など）を書く
  3. **Gitにコミット：** ターミナルで`git add .cursorrules`と入力してEnter。次に`git commit -m "Add .cursorrules for team"`と入力してEnter
  4. **リモートリポジトリにプッシュ（必要なとき）：** `git push`でリモートリポジトリにプッシュする
  5. **チームメンバーがリポジトリをクローン：** チームメンバーが`git clone`でリポジトリをクローンすると、**自動で`.cursorrules`が含まれる**
  6. **全員が同じルールを使う：** チームメンバー全員が同じ`.cursorrules`を使うことで、**コードスタイルが統一**される

**【マンガパート】**

**マンガシーン3-2（ページ6-7）：**
- **パネル1：** デイビッドが「`.cursorrules`を共有するときの**ベストプラクティス**を覚えよう」と説明する。
- **デイビッド：** 「(1) **わかりやすく書く**：チームメンバー全員が理解できるように、**具体的に書く**。(2) **定期的に更新する**：プロジェクトのルールが変わったら、`.cursorrules`も更新する。(3) **レビューする**：`.cursorrules`を変更するときは、チームメンバーと**レビュー**してからコミットする。」

**パネル2：** アレックスが「`.cursorrules`を更新するときは、どうすればいいんですか？」と聞く。
- **デイビッド：** 「`.cursorrules`を更新したら、**Gitにコミット**して、チームメンバーに**プルしてもらう**。変更内容を**コミットメッセージに書く**と、チームメンバーが**何が変わったか**わかりやすいよ。」

**パネル3：** アレックスが「チームメンバーが`.cursorrules`を更新したら、どうすればいいんですか？」と聞く。
- **デイビッド：** 「`git pull`でリポジトリを更新すると、**最新の`.cursorrules`が自動で取得**される。Cursorを再起動すると、**新しいルールが反映**されるよ。」

**【インフォグラフィックパート】**

**ページ7-8：**
- **`.cursorrules`を共有するベストプラクティス：**
  | 項目 | 内容 |
  |------|------|
  | **わかりやすく書く** | チームメンバー全員が理解できるように、**具体的に書く** |
  | **定期的に更新する** | プロジェクトのルールが変わったら、`.cursorrules`も更新する |
  | **レビューする** | `.cursorrules`を変更するときは、チームメンバーと**レビュー**してからコミットする |
  | **コミットメッセージに書く** | 変更内容を**コミットメッセージに書く**と、チームメンバーが**何が変わったか**わかりやすい |
  | **プルして反映** | `git pull`でリポジトリを更新すると、**最新の`.cursorrules`が自動で取得**される。Cursorを再起動すると、**新しいルールが反映**される |
- **ポイント：** `.cursorrules`を共有することで、**チーム全体で一貫性**が保てる。定期的に更新して、**チームメンバー全員が最新のルール**を使うようにする。

---

### **セクション17.2：Cursorをコードレビューで活用する（ページ9-15）**

**【マンガパート】**

**マンガシーン4-1（ページ9）：**
- **パネル1：** デイビッドが「Cursorを**コードレビュー**で活用すると、**変更内容を説明**したり、**潜在的な問題を指摘**したりできるよ」と説明する。
- **デイビッド：** 「コードレビューで、**変更内容を説明する**のが大変なときは、Cursorの**Chat**に変更内容を貼り付けて、**『この変更は何をしていますか？潜在的な問題はありますか？』**って聞けば、**わかりやすく説明**してくれるよ。」

**パネル2：** アレックスが「コードレビューで、Cursorをどう使えばいいんですか？」と聞く。
- **デイビッド：** 「(1) **変更内容を説明してもらう**：Chatに変更内容を貼り付けて、**『この変更は何をしていますか？』**って聞く。(2) **潜在的な問題を指摘してもらう**：**『この変更に潜在的な問題はありますか？』**って聞く。(3) **改善提案をもらう**：**『この変更を改善する方法はありますか？』**って聞く。」

**パネル3：** アレックスが「`daily_log`で、コードレビューの流れを体験してみたい」と言う。
- **デイビッド：** 「じゃあ、`log.py`に変更を加えて、Cursorでコードレビューする流れを体験してみよう。」

**【マンガパート】**

**マンガシーン4-2（ページ10-11）：**
- **パネル1：** アレックスが`log.py`に変更を加える（例：`--day`機能に、エラーハンドリングを追加する）。
- **デイビッド：** 「まず、`log.py`に変更を加える。例として、`--day`機能に、**エラーハンドリング**を追加してみよう。」

**パネル2：** アレックスがCursorの**Chat**（`⌘+L`／`Ctrl+L`）を開き、変更内容を貼り付けて質問する。
- **アレックス（入力）：** 「この変更は何をしていますか？潜在的な問題はありますか？改善する方法はありますか？」
- **デイビッド：** 「変更内容を**コピー**して、Chatに**貼り付け**て、**具体的な質問**をする。Cursorが**変更内容を説明**して、**潜在的な問題を指摘**して、**改善提案**をしてくれるよ。」
- **視覚的表現：** Chatパネルが開き、アレックスが変更内容を貼り付けて質問する様子。

**パネル3：** Cursorが変更内容を説明し、潜在的な問題を指摘し、改善提案をする。
- **Cursor（応答）：** 「この変更は、`--day`機能にエラーハンドリングを追加しています。潜在的な問題として、**日付の形式が間違っている場合の処理**が不足しています。改善方法として、**日付の形式を検証する処理**を追加することをおすすめします。」
- **アレックス：** 「Cursorが**変更内容を説明**して、**潜在的な問題を指摘**して、**改善提案**をしてくれた。コードレビューで使えそう。」

**【インフォグラフィックパート】**

**ページ11-12：**
- **Cursorをコードレビューで活用する方法：**
  | 用途 | 質問の例 |
  |------|----------|
  | **変更内容を説明してもらう** | 「この変更は何をしていますか？」 |
  | **潜在的な問題を指摘してもらう** | 「この変更に潜在的な問題はありますか？」 |
  | **改善提案をもらう** | 「この変更を改善する方法はありますか？」 |
- **ポイント：** Cursorをコードレビューで活用することで、**変更内容を説明**したり、**潜在的な問題を指摘**したり、**改善提案**をもらったりできる。

**【テキストパート - 詳細説明】**

**ページ12、下部：**
- **Cursorをコードレビューで活用する完全手順（そのまま再現できる）：**
  1. **変更内容をコピー：** 変更したコードを**マウスで選択**して、`⌘+C`（Mac）／`Ctrl+C`（Win）でコピーする
  2. **Chatを開く：** `⌘+L`（Mac）／`Ctrl+L`（Win）を押すか、メニュー「**View**」→「**Command Palette**」→「**Cursor: Open Chat**」をクリックする
  3. **変更内容を貼り付けて質問：** Chatの入力欄に変更内容を**貼り付け**（`⌘+V`／`Ctrl+V`）して、**「この変更は何をしていますか？潜在的な問題はありますか？改善する方法はありますか？」**と入力する
  4. **Sendボタンをクリック：** 入力欄の右側にある**Send**ボタン（または`Enter`キー）をクリックする
  5. **Cursorの説明を読む：** Cursorが**変更内容を説明**して、**潜在的な問題を指摘**して、**改善提案**をする。**必ず目で確認**して、**なぜその問題があるか・どう改善すればいいか**を理解する
  6. **改善提案を実装（必要なとき）：** 改善提案を実装する場合は、**Composer**（`⌘+I`／`Ctrl+I`）で指示を出す

**【マンガパート】**

**マンガシーン4-3（ページ13-14）：**
- **パネル1：** デイビッドが「コードレビューで、**Composer**も活用できるよ」と説明する。
- **デイビッド：** 「改善提案を実装するときは、**Composer**に**『この変更を改善してください。日付の形式を検証する処理を追加してください』**って指示すれば、**自動で改善**してくれるよ。」

**パネル2：** アレックスがComposer（`⌘+I`／`Ctrl+I`）を開き、改善提案の指示を入力する。
- **アレックス（入力）：** 「`log.py`の`--day`機能に、日付の形式を検証する処理を追加してください。`YYYY-MM-DD`形式でない場合は、エラーメッセージを表示してください。」
- **視覚的表現：** Composerパネルが開き、アレックスが改善提案の指示を入力する様子。

**パネル3：** Cursorが改善提案を実装し、アレックスが**Accept**ボタンをクリックする。
- **アレックス：** 「Cursorが**自動で改善**してくれた。コードレビューで、**変更内容を説明**して、**潜在的な問題を指摘**して、**改善提案を実装**までできるんだ。」

**【インフォグラフィックパート】**

**ページ14-15：**
- **コードレビューでのCursor活用の流れ：**
  1. **変更内容をコピー：** 変更したコードをコピーする
  2. **Chatで質問：** Chatに変更内容を貼り付けて、**「この変更は何をしていますか？潜在的な問題はありますか？」**と質問する
  3. **改善提案を確認：** Cursorの改善提案を確認する
  4. **Composerで実装（必要なとき）：** 改善提案を実装する場合は、**Composer**で指示を出す
  5. **再レビュー：** 改善後のコードを再レビューする
- **ポイント：** Cursorをコードレビューで活用することで、**変更内容を説明**したり、**潜在的な問題を指摘**したり、**改善提案を実装**したりできる。

---

### **セクション17.3：チーム開発でのベストプラクティス（ページ15-19）**

**【マンガパート】**

**マンガシーン5-1（ページ15）：**
- **パネル1：** デイビッドが「チーム開発での**ベストプラクティス**を覚えよう」と説明する。
- **デイビッド：** 「(1) **`.cursorrules`を定期的に更新する**：プロジェクトのルールが変わったら、`.cursorrules`も更新する。(2) **コードスタイルを統一する**：`.cursorrules`を共有することで、**コードスタイルが統一**される。(3) **コミュニケーションを取る**：`.cursorrules`を変更するときは、チームメンバーと**コミュニケーション**を取る。」

**パネル2：** アレックスが「チーム開発で、Cursorをどう使えばいいんですか？」と聞く。
- **デイビッド：** 「(1) **コードレビューで活用**：変更内容を説明したり、潜在的な問題を指摘したりする。(2) **`.cursorrules`を共有**：チームメンバー全員が同じルールを使う。(3) **定期的に更新**：`.cursorrules`を定期的に更新して、**チームメンバー全員が最新のルール**を使うようにする。」

**パネル3：** アレックスが「チーム開発でのベストプラクティスを、まとめて教えてください」と言う。
- **デイビッド：** 「じゃあ、チーム開発でのベストプラクティスを、**表形式**でまとめよう。」

**【インフォグラフィックパート】**

**ページ15-16：**
- **チーム開発でのベストプラクティス：**
  | 項目 | 内容 |
  |------|------|
  | **`.cursorrules`を定期的に更新する** | プロジェクトのルールが変わったら、`.cursorrules`も更新する |
  | **コードスタイルを統一する** | `.cursorrules`を共有することで、**コードスタイルが統一**される |
  | **コミュニケーションを取る** | `.cursorrules`を変更するときは、チームメンバーと**コミュニケーション**を取る |
  | **コードレビューでCursorを活用** | 変更内容を説明したり、潜在的な問題を指摘したりする |
  | **改善提案を実装** | 改善提案を実装する場合は、**Composer**で指示を出す |
- **ポイント：** チーム開発では、**`.cursorrules`を共有**して、**Cursorをコードレビューで活用**することで、**コードスタイルが統一**され、**コードレビューが効率的**になる。

**【マンガパート】**

**マンガシーン5-2（ページ16-17）：**
- **パネル1：** アレックスが「チーム開発で、Cursorを使うときの**注意点**はありますか？」と聞く。
- **デイビッド：** 「(1) **`.cursorrules`を共有する**：チームメンバー全員が同じルールを使うようにする。(2) **定期的に更新する**：`.cursorrules`を定期的に更新して、**チームメンバー全員が最新のルール**を使うようにする。(3) **コミュニケーションを取る**：`.cursorrules`を変更するときは、チームメンバーと**コミュニケーション**を取る。」

**パネル2：** アレックスが「チーム開発で、Cursorを使うときの**良い例・悪い例**を教えてください」と言う。
- **デイビッド：** 「**良い例**：(1) `.cursorrules`をGitリポジトリに含めて、チームメンバー全員が同じルールを使う。(2) コードレビューでCursorを活用して、変更内容を説明したり、潜在的な問題を指摘したりする。(3) `.cursorrules`を定期的に更新して、**チームメンバー全員が最新のルール**を使うようにする。**悪い例**：(1) `.cursorrules`を共有せずに、各自が独自のルールを使う。(2) コードレビューでCursorを使わずに、手動でレビューする。(3) `.cursorrules`を更新せずに、古いルールを使い続ける。」

**パネル3：** アレックスが「チーム開発でのベストプラクティスを、実践してみたい」と言う。
- **デイビッド：** 「じゃあ、`daily_log`プロジェクトで、`.cursorrules`を共有して、コードレビューでCursorを活用する流れを、もう一度体験してみよう。」

**【インフォグラフィックパート】**

**ページ17-18：**
- **良い例 vs 悪い例：**
  | 項目 | 良い例 | 悪い例 |
  |------|--------|--------|
  | **`.cursorrules`の共有** | Gitリポジトリに含めて、チームメンバー全員が同じルールを使う | `.cursorrules`を共有せずに、各自が独自のルールを使う |
  | **コードレビュー** | Cursorを活用して、変更内容を説明したり、潜在的な問題を指摘したりする | Cursorを使わずに、手動でレビューする |
  | **`.cursorrules`の更新** | 定期的に更新して、**チームメンバー全員が最新のルール**を使うようにする | 更新せずに、古いルールを使い続ける |
- **ポイント：** チーム開発では、**`.cursorrules`を共有**して、**Cursorをコードレビューで活用**することで、**コードスタイルが統一**され、**コードレビューが効率的**になる。

---

### **章のまとめ（ページ19-20）**

**【マンガパート】**

**マンガシーン6（ページ19）：**
- **パネル1：** アレックスが`daily_log`プロジェクトを開いたまま、`.cursorrules`ファイルとコードレビューの結果を見ている。
- **アレックス：** 「チーム開発で、**`.cursorrules`を共有**して、**Cursorをコードレビューで活用**することで、**コードスタイルが統一**され、**コードレビューが効率的**になった。」
- **デイビッド：** 「その感覚でOK。チーム開発では、**`.cursorrules`を共有**して、**Cursorをコードレビューで活用**することで、**チーム全体で一貫性**が保てるよ。」

**パネル2：** デイビッドが章のポイントをまとめる。
- **デイビッド：** 「この章では、(1) **`.cursorrules`をチームで共有**する方法（Gitリポジトリに含める、共有のベストプラクティス）、(2) **Cursorをコードレビューで活用**する方法（変更内容の説明、潜在的な問題の指摘、改善提案）、(3) **チーム開発でのベストプラクティス**（`.cursorrules`の更新、コードスタイルの統一、コミュニケーション）、ってところだね。」
- **アレックス：** 「はい。チーム開発で、Cursorをどう使えばいいかわかった。」

**【インフォグラフィックパート】**

**ページ19-20：**
- 第17章のまとめ：
  - ✅ **`.cursorrules`をチームで共有**する方法：Gitリポジトリに含める、共有のベストプラクティス
  - ✅ **Cursorをコードレビューで活用**する方法：変更内容の説明、潜在的な問題の指摘、改善提案
  - ✅ **チーム開発でのベストプラクティス**：`.cursorrules`の更新、コードスタイルの統一、コミュニケーション
  - ✅ **良い例 vs 悪い例**：`.cursorrules`の共有、コードレビュー、`.cursorrules`の更新
- **実践チェックリスト（今すぐ試す）：**
  - [ ] `daily_log`プロジェクトのルートディレクトリに`.cursorrules`ファイルを作成する
  - [ ] `.cursorrules`ファイルにプロジェクトのルールを書く
  - [ ] Gitにコミットする（`git add .cursorrules`、`git commit -m "Add .cursorrules for team"`）
  - [ ] `log.py`に変更を加えて、Cursorの**Chat**でコードレビューする
  - [ ] 改善提案を**Composer**で実装する

**【マンガパート】**

**マンガシーン6-2（ページ20）：**
- **パネル1：** アレックスとデイビッドが次の章に向けて会話する。
- **アレックス：** 「次は何をしますか？」
- **デイビッド：** 「**Chapter 18**では、**よくある問題の解決法**を詳しくやる。エラーが発生したときの対処法、パフォーマンスの問題、セキュリティのベストプラクティスを、`daily_log`で実際に問題を出して、解決していく流れで学ぶよ。」

**【インフォグラフィックパート】**

**ページ20：**
- 次章の予告：
  - **Chapter 18: よくある問題の解決法**
  - エラーが発生したときの対処法、パフォーマンスの問題、セキュリティのベストプラクティス
  - `daily_log`で実際に問題を出して、解決していく流れ

---

## ✅ 確認ポイント

この日本語訳で確認すべき点（第1章と同じく、**丁寧さ・わかりやすさ・実践できる**を基準に）：

1. **問題の明確さ：** 「チーム開発で、コードスタイルがバラバラ、コードレビューで変更内容を説明するのが大変」という困りごとが、読者に伝わるか？
2. **`.cursorrules`の共有方法：** Gitリポジトリに含める、**具体的な操作**（EXPLORERで右クリック→「New File」→`.cursorrules`と入力、`git add`、`git commit`）が、読者が**そのまま手順を辿って**試せるか？
3. **コードレビューでのCursor活用：** 変更内容を貼り付けて質問する、**具体的な操作**（Chatの開き方、貼り付け、Sendボタン）が、読者が**そのまま手順を辿って**試せるか？
4. **詳細説明：** 「テキストパート - 詳細説明」で、**EXPLORERでのファイル作成**、**Gitコマンド**、**Chatの開き方**、**Sendボタンの位置**など、**完全な再現手順**が書けているか？
5. **実例の親しみやすさ：** `daily_log`プロジェクトを使った`.cursorrules`の共有とコードレビューの実例が、初心者にも試しやすいか？
6. **ベストプラクティスの明確さ：** **表形式**で整理されたベストプラクティスと、**良い例・悪い例**の比較が、第1章の「良い例・悪い例」と同じレベルでわかりやすいか？
7. **Chapter 12との整合：** `.cursorrules`の説明が、Chapter 12と矛盾せず、**チーム開発での活用**として機能しているか？
8. **次章へのつながり：** Chapter 18のよくある問題の解決法（エラー対処、パフォーマンス、セキュリティ）への自然な導入になっているか？

---

**第17章の確認用日本語訳 - 完了**
