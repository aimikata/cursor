# 第12章：.cursorrules：AIにプロジェクトのルールを教える（確認用日本語訳）

> **注意：** これは確認用の日本語訳です。最終出版は英語版です。
> ストーリーの流れと内容の正確性を確認するために使用してください。

## 📖 章の概要

**章タイトル：** .cursorrules：AIにプロジェクトのルールを教える  
**ページ数：** 18-20ページ（視覚学習に最適化）  
**学習目標：**
- 「毎回同じことを説明するのは時間の無駄」「.cursorrulesを作る必要はあるの？」という問題を理解する
- **なぜ.cursorrulesが必要か**を理解する（毎回同じことを説明するのは時間の無駄、チームで共有できる、一貫性が保てる、など）
- `.cursorrules`ファイルを作成し、プロジェクトのルールをCursorに教える方法を学ぶ
- チームで`.cursorrules`を共有する方法を学ぶ

---

## 🎬 ストーリー構造

### **オープニングシーン（ページ1-2）：問題は何か？**

**【マンガパート】**

**設定：** アレックスのアパート。`daily_log`プロジェクトを開き、Cursorの**Chat**や**Composer**を使っている。毎回同じことを説明するのが面倒だと感じている。

**マンガシーン1（ページ1）：**
- **パネル1：** アレックスがCursorの**Chat**で「`log.py`のコードスタイルを統一して。関数名は`snake_case`で、コメントは日本語で書いて」と指示する。
- **アレックス（思考）：** 「毎回、**同じことを説明するの面倒**だな…。コードスタイルとか、コメントの書き方とか、**毎回同じことを言ってる**気がする。」

**パネル2：** アレックスがCursorの**Composer**で「`log.py`に新しい機能を追加して。関数名は`snake_case`で、コメントは日本語で書いて」と指示する。
- **アレックス（思考）：** 「また同じことを説明した…。**毎回同じことを説明するのは時間の無駄**だな。もっと効率的にできないかな？」

**パネル3：** デイビッドがビデオ通話で現れる。
- **デイビッド：** 「それ、**`.cursorrules`**ファイルを作れば解決できるよ。プロジェクトのルール（コードスタイル、コメントの書き方、など）を`.cursorrules`ファイルに書いておくと、Cursorが**自動で理解**してくれる。毎回同じことを説明する必要がなくなるよ。」
- **アレックス：** 「`.cursorrules`って、どう作るんですか？それに、**毎回説明すればいいのでは？.cursorrulesを作る必要はあるの？**」

**【インフォグラフィックパート】**

**ページ1-2、下部：**
- **この章で学ぶこと：** `.cursorrules`ファイルの作成方法、プロジェクトのルールをCursorに教える方法
- **問題：** 毎回同じことを説明するのは時間の無駄、**毎回説明すればいいのでは？.cursorrulesを作る必要はあるの？**
- **解決：** **なぜ.cursorrulesが必要か**を理解する → `.cursorrules`ファイルを作成してプロジェクトのルールを書く → Cursorが自動で理解してくれる
- **メリット：** (1) **毎回同じことを説明する必要がなくなる**（時間が省ける）、(2) **チームで共有できる**（チームメンバー全員が同じルールを使える）、(3) **一貫性が保てる**（プロジェクト全体で同じコードスタイルになる）、(4) **プロジェクトのルールが明確になる**（`.cursorrules`ファイルを見れば、どんなルールがあるかわかる）
- **使うもの：** `.cursorrules`ファイル、Cursorの**Chat**（`⌘+L`／`Ctrl+L`）、**Composer**（`⌘+I`／`Ctrl+I`）

---

### **セクション12.1：問題ー「毎回同じことを説明するのは時間の無駄」「.cursorrulesを作る必要はあるの？」（ページ3-4）**

**【マンガパート】**

**マンガシーン2（ページ3）：**
- **パネル1：** デイビッドが「まず、**なぜ.cursorrulesが必要か**を理解しよう」と説明する。
- **デイビッド：** 「毎回同じことを説明するのは**時間がかかる**し、**見落としがある**可能性がある。`.cursorrules`ファイルを作っておくと、Cursorが**自動で理解**してくれるから、**毎回同じことを説明する必要がなくなる**んだ。」

**パネル2：** アレックスが「でも、**毎回説明すればいいのでは？.cursorrulesを作る必要はあるの？**」と聞く。
- **デイビッド：** 「毎回説明するのは**時間の無駄**だし、**見落としがある**可能性がある。`.cursorrules`ファイルを作っておくと、(1) **毎回同じことを説明する必要がなくなる**（時間が省ける）、(2) **チームで共有できる**（チームメンバー全員が同じルールを使える）、(3) **一貫性が保てる**（プロジェクト全体で同じコードスタイルになる）、(4) **プロジェクトのルールが明確になる**（`.cursorrules`ファイルを見れば、どんなルールがあるかわかる）、ってメリットがあるよ。」

**パネル3：** アレックスが「`.cursorrules`ファイルって、どう作るんですか？」と聞く。
- **デイビッド：** 「プロジェクトのルートディレクトリに`.cursorrules`ファイルを作って、プロジェクトのルールを書くだけ。Cursorの**Chat**や**Composer**に『`.cursorrules`ファイルを作成して、プロジェクトのルールを書いて』って指示すれば、自動生成してくれるよ。」

**【インフォグラフィックパート】**

**ページ3-4：**
- **問題の整理：**
  - 毎回同じことを説明するのは時間の無駄
  - **毎回説明すればいいのでは？.cursorrulesを作る必要はあるの？**
  - `.cursorrules`を作らないとどうなるかわからない
- **解決の考え方：** **なぜ.cursorrulesが必要か**を理解する → `.cursorrules`ファイルを作成してプロジェクトのルールを書く → Cursorが自動で理解してくれる
- **.cursorrulesを作らないとどうなるか：**
  - **毎回同じことを説明する必要がある**（時間がかかる）
  - **見落としがある**可能性がある（説明を忘れることがある）
  - **チームで共有できない**（チームメンバーが同じルールを使えない）
  - **一貫性が保てない**（プロジェクト全体でコードスタイルがバラバラになる）
  - **プロジェクトのルールが明確にならない**（どんなルールがあるかわからない）
- **.cursorrulesを作るとどうなるか：**
  - **毎回同じことを説明する必要がなくなる**（時間が省ける）
  - **見落としがなくなる**（`.cursorrules`ファイルに書いてあるから、忘れない）
  - **チームで共有できる**（チームメンバー全員が同じルールを使える）
  - **一貫性が保てる**（プロジェクト全体で同じコードスタイルになる）
  - **プロジェクトのルールが明確になる**（`.cursorrules`ファイルを見れば、どんなルールがあるかわかる）

---

### **セクション12.2：なぜ.cursorrulesが必要か（ページ5-8）**

**【マンガパート】**

**マンガシーン3（ページ5）：**
- **パネル1：** デイビッドが「**.cursorrulesを作らないとどうなるか**を具体的に見てみよう」と説明する。
- **デイビッド：** 「例えば、毎回Cursorの**Chat**や**Composer**で『関数名は`snake_case`で、コメントは日本語で書いて』って説明するのは**時間がかかる**し、**見落としがある**可能性がある。`.cursorrules`ファイルを作っておくと、**自動で理解**してくれるから、**毎回同じことを説明する必要がなくなる**んだ。」

**パネル2：** アレックスが「でも、**毎回説明すればいいのでは？.cursorrulesを作る必要はあるの？**」と聞く。
- **デイビッド：** 「毎回説明するのは**時間の無駄**だし、**見落としがある**可能性がある。それに、**チームで共有できない**から、チームメンバーが同じルールを使えない。`.cursorrules`ファイルを作っておくと、**チームメンバー全員が同じルールを使える**から、**一貫性が保てる**よ。」

**パネル3：** アレックスが「`.cursorrules`を作ると、どんなメリットがあるんですか？」と聞く。
- **デイビッド：** 「`.cursorrules`を作ると、(1) **毎回同じことを説明する必要がなくなる**（時間が省ける）、(2) **チームで共有できる**（チームメンバー全員が同じルールを使える）、(3) **一貫性が保てる**（プロジェクト全体で同じコードスタイルになる）、(4) **プロジェクトのルールが明確になる**（`.cursorrules`ファイルを見れば、どんなルールがあるかわかる）、ってメリットがあるよ。」

**【インフォグラフィックパート】**

**ページ5-8：**
- **.cursorrulesを作らないとどうなるか：**
  | 状況 | 問題 | 結果 |
  |------|------|------|
  | **毎回説明するとき** | 時間がかかる、見落としがある | **効率が悪い** |
  | **チームで共有するとき** | チームメンバーが同じルールを使えない | **一貫性が保てない** |
  | **プロジェクトのルールを確認するとき** | どんなルールがあるかわからない | **ルールが明確にならない** |
  | **新しいメンバーが参加したとき** | ルールを説明する必要がある | **時間がかかる** |

- **.cursorrulesを作るとどうなるか：**
  | 状況 | メリット | 結果 |
  |------|----------|------|
  | **毎回説明するとき** | 毎回同じことを説明する必要がなくなる（時間が省ける） | **効率が良くなる** |
  | **チームで共有するとき** | チームメンバー全員が同じルールを使える | **一貫性が保てる** |
  | **プロジェクトのルールを確認するとき** | `.cursorrules`ファイルを見れば、どんなルールがあるかわかる | **ルールが明確になる** |
  | **新しいメンバーが参加したとき** | `.cursorrules`ファイルを見れば、ルールがわかる | **時間が省ける** |

- **.cursorrulesのメリット：**
  1. **毎回同じことを説明する必要がなくなる**（時間が省ける）
  2. **チームで共有できる**（チームメンバー全員が同じルールを使える）
  3. **一貫性が保てる**（プロジェクト全体で同じコードスタイルになる）
  4. **プロジェクトのルールが明確になる**（`.cursorrules`ファイルを見れば、どんなルールがあるかわかる）

---

### **セクション12.3：.cursorrulesファイルを作成する（ページ9-14）**

**【マンガパート】**

**マンガシーン4（ページ9）：**
- **パネル1：** デイビッドが「じゃあ、`daily_log`プロジェクトの`.cursorrules`ファイルを作ってみよう」と説明する。
- **デイビッド：** 「プロジェクトのルートディレクトリに`.cursorrules`ファイルを作って、プロジェクトのルールを書く。例えば、コードスタイル、コメントの書き方、関数名の規則、など。Cursorの**Chat**や**Composer**に『`.cursorrules`ファイルを作成して、`daily_log`プロジェクトのルールを書いて。関数名は`snake_case`で、コメントは日本語で書いて』って指示すれば、自動生成してくれるよ。」

**パネル2：** アレックスが「`.cursorrules`ファイルには、どんなことを書けばいいんですか？」と聞く。
- **デイビッド：** 「プロジェクトのルールを書く。例えば、(1) **コードスタイル**（関数名は`snake_case`、変数名は`snake_case`、など）、(2) **コメントの書き方**（コメントは日本語で書く、など）、(3) **関数の書き方**（関数にはdocstringを書く、など）、(4) **ファイル構造**（テストファイルは`test_`で始める、など）、って感じだよ。」

**パネル3：** アレックスが「`daily_log`で実際に`.cursorrules`ファイルを作ってみたい」と身を乗り出す。
- **デイビッド：** 「じゃあ、Cursorの**Chat**で『`.cursorrules`ファイルを作成して、`daily_log`プロジェクトのルールを書いて』って聞いてみよう。その後、**Composer**で`.cursorrules`ファイルを作成する流れを体験してみて。」

**【インフォグラフィックパート】**

**ページ9-10：**
- **.cursorrulesファイルを作成する流れ：**
  1. **Chatで質問：** Chat（`⌘+L`／`Ctrl+L`）を開き、**「`.cursorrules`ファイルを作成して、`daily_log`プロジェクトのルールを書いて。関数名は`snake_case`で、コメントは日本語で書いて」**と質問する。**Send**ボタンをクリックする
  2. **Cursorの提案を確認：** Cursorが`.cursorrules`ファイルの提案を表示する。**必ず目で確認**して、**どんなルールが書かれているか**を理解する
  3. **Composerで生成：** Composer（`⌘+I`／`Ctrl+I`）を開き、**「`.cursorrules`ファイルを作成して、`daily_log`プロジェクトのルールを書いて。関数名は`snake_case`で、コメントは日本語で書いて」**と指示する。**Generate**ボタンをクリックする
  4. **生成された.cursorrulesを確認：** Composerパネルに表示された`.cursorrules`ファイルの内容を**必ず目で確認**する。**どんなルールが書かれているか**を理解する

**ページ11-12：**
- **実践：Chatで.cursorrules作成を相談する（ページ11-12）**

**【マンガパート】**

**マンガシーン5（ページ11）：**
- **パネル1：** アレックスがCursorの**Chat**（`⌘+L`／`Ctrl+L`）を開き、**「`.cursorrules`ファイルを作成して、`daily_log`プロジェクトのルールを書いて。関数名は`snake_case`で、コメントは日本語で書いて」**と入力する。
- **アレックス（入力）：** 「`.cursorrules`ファイルを作成して、`daily_log`プロジェクトのルールを書いて。関数名は`snake_case`で、コメントは日本語で書いて」

**パネル2：** Cursorが`.cursorrules`ファイルの提案を表示する。
- **Cursor（応答）：** 「`.cursorrules`ファイルを作成して、以下のルールを書きます：
  ```
  # daily_log プロジェクトのルール
  
  ## コードスタイル
  - 関数名は`snake_case`を使用する
  - 変数名は`snake_case`を使用する
  - コメントは日本語で書く
  
  ## 関数の書き方
  - 関数にはdocstringを書く（日本語で）
  - 関数は1つのことをするようにする
  
  ## ファイル構造
  - テストファイルは`test_`で始める
  - メインのコードは`log.py`に書く
  ```」

**パネル3：** アレックスが「`.cursorrules`ファイルが生成されたけど、**どんなルールが書かれているか**がわからない…」と困る。
- **デイビッド：** 「大丈夫。Cursorの**Chat**に**『この.cursorrulesファイルには、どんなルールが書かれていますか？』**って聞けば、わかりやすく説明してくれるよ。」

**【インフォグラフィックパート】**

**ページ11-12：**
- **Chatで.cursorrules作成を相談する手順：**
  1. **Chatを開く：** Chat（`⌘+L`／`Ctrl+L`）を開く
  2. **質問を入力：** **「`.cursorrules`ファイルを作成して、`daily_log`プロジェクトのルールを書いて。関数名は`snake_case`で、コメントは日本語で書いて」**と入力する
  3. **Sendボタンをクリック：** **Send**ボタンをクリックする
  4. **Cursorの提案を確認：** Cursorが`.cursorrules`ファイルの提案を表示する。**必ず目で確認**して、**どんなルールが書かれているか**を理解する
  5. **.cursorrulesファイルの説明を聞く：** 必要に応じて、Cursorの**Chat**に**「この.cursorrulesファイルには、どんなルールが書かれていますか？」**と質問する

**ページ13-14：**
- **実践：Composerで.cursorrulesファイルを作成する（ページ13-14）**

**【マンガパート】**

**マンガシーン6（ページ13）：**
- **パネル1：** アレックスがCursorの**Composer**（`⌘+I`／`Ctrl+I`）を開き、**「`.cursorrules`ファイルを作成して、`daily_log`プロジェクトのルールを書いて。関数名は`snake_case`で、コメントは日本語で書いて」**と指示する。
- **アレックス（入力）：** 「`.cursorrules`ファイルを作成して、`daily_log`プロジェクトのルールを書いて。関数名は`snake_case`で、コメントは日本語で書いて」

**パネル2：** Composerが`.cursorrules`ファイルを生成する。
- **Composer（提案）：** `.cursorrules`ファイルを作成し、以下の内容を提案する：
  ```
  # daily_log プロジェクトのルール
  
  ## コードスタイル
  - 関数名は`snake_case`を使用する
  - 変数名は`snake_case`を使用する
  - コメントは日本語で書く
  
  ## 関数の書き方
  - 関数にはdocstringを書く（日本語で）
  - 関数は1つのことをするようにする
  
  ## ファイル構造
  - テストファイルは`test_`で始める
  - メインのコードは`log.py`に書く
  ```

**パネル3：** アレックスが「生成された`.cursorrules`ファイルを確認したい」と言う。
- **デイビッド：** 「Composerパネルに表示された`.cursorrules`ファイルの内容を**必ず目で確認**して、**どんなルールが書かれているか**を理解しよう。その後、**Accept**ボタンをクリックして、`.cursorrules`ファイルを作成するよ。」

**【インフォグラフィックパート】**

**ページ13-14：**
- **Composerで.cursorrulesファイルを作成する手順：**
  1. **Composerを開く：** Composer（`⌘+I`／`Ctrl+I`）を開く
  2. **指示を入力：** **「`.cursorrules`ファイルを作成して、`daily_log`プロジェクトのルールを書いて。関数名は`snake_case`で、コメントは日本語で書いて」**と指示する
  3. **Generateボタンをクリック：** **Generate**ボタンをクリックする
  4. **生成された.cursorrulesを確認：** Composerパネルに表示された`.cursorrules`ファイルの内容を**必ず目で確認**する。**どんなルールが書かれているか**を理解する
  5. **Acceptボタンをクリック：** **Accept**ボタンをクリックして、`.cursorrules`ファイルを作成する

- **生成された.cursorrulesファイルの確認ポイント：**
  - **どんなルールが書かれているか**（コードスタイル、コメントの書き方、関数の書き方、ファイル構造、など）
  - **ルールが適切か**（プロジェクトに合っているか）
  - **ルールが明確か**（わかりやすいか）

---

### **セクション12.4：チームで.cursorrulesを共有する（ページ15-18）**

**【マンガパート】**

**マンガシーン7（ページ15）：**
- **パネル1：** デイビッドが「`.cursorrules`ファイルを作ったら、**チームで共有**しよう」と説明する。
- **デイビッド：** 「`.cursorrules`ファイルをGitにコミットして、チームメンバー全員が同じルールを使えるようにする。そうすれば、**チーム全体で一貫性が保てる**し、**新しいメンバーが参加したとき**も、`.cursorrules`ファイルを見れば、ルールがわかるよ。」

**パネル2：** アレックスが「チームで共有すると、どんなメリットがあるんですか？」と聞く。
- **デイビッド：** 「チームで共有すると、(1) **チームメンバー全員が同じルールを使える**（一貫性が保てる）、(2) **新しいメンバーが参加したとき**も、`.cursorrules`ファイルを見れば、ルールがわかる（時間が省ける）、(3) **プロジェクトのルールが明確になる**（`.cursorrules`ファイルを見れば、どんなルールがあるかわかる）、ってメリットがあるよ。」

**パネル3：** アレックスが「`.cursorrules`ファイルをGitにコミットする方法を教えてください」と言う。
- **デイビッド：** 「`.cursorrules`ファイルをGitにコミットする方法は、普通のファイルと同じ。`git add .cursorrules`でステージングして、`git commit -m "Add .cursorrules"`でコミットするだけ。チームメンバーが`git pull`すれば、`.cursorrules`ファイルが共有されるよ。」

**【インフォグラフィックパート】**

**ページ15-18：**
- **チームで.cursorrulesを共有する手順：**
  1. **.cursorrulesファイルを作成：** プロジェクトのルートディレクトリに`.cursorrules`ファイルを作成する
  2. **Gitにコミット：** `git add .cursorrules`でステージングして、`git commit -m "Add .cursorrules"`でコミットする
  3. **リモートリポジトリにプッシュ：** `git push`でリモートリポジトリにプッシュする
  4. **チームメンバーがプル：** チームメンバーが`git pull`すれば、`.cursorrules`ファイルが共有される

- **チームで共有するメリット：**
  | 状況 | メリット | 結果 |
  |------|----------|------|
  | **チームメンバー全員が同じルールを使うとき** | 一貫性が保てる | **プロジェクト全体で同じコードスタイルになる** |
  | **新しいメンバーが参加したとき** | `.cursorrules`ファイルを見れば、ルールがわかる | **時間が省ける** |
  | **プロジェクトのルールを確認するとき** | `.cursorrules`ファイルを見れば、どんなルールがあるかわかる | **ルールが明確になる** |

- **.cursorrulesファイルの例：**
  ```
  # daily_log プロジェクトのルール
  
  ## コードスタイル
  - 関数名は`snake_case`を使用する
  - 変数名は`snake_case`を使用する
  - コメントは日本語で書く
  
  ## 関数の書き方
  - 関数にはdocstringを書く（日本語で）
  - 関数は1つのことをするようにする
  
  ## ファイル構造
  - テストファイルは`test_`で始める
  - メインのコードは`log.py`に書く
  ```

---

## 📝 章のまとめ（ページ19-20）

**【マンガパート】**

**マンガシーン8（ページ19）：**
- **パネル1：** デイビッドが「この章では、(1) **なぜ.cursorrulesが必要か**を理解した（毎回同じことを説明するのは時間の無駄、チームで共有できる、一貫性が保てる、など）、(2) `.cursorrules`ファイルを作成し、プロジェクトのルールをCursorに教える方法を学んだ、(3) チームで`.cursorrules`を共有する方法を学んだ、ってところだね。」
- **アレックス：** 「`.cursorrules`ファイルを作っておくと、**毎回同じことを説明する必要がなくなる**んですね。**チームで共有**できるのも便利ですね。」

**パネル2：** アレックスが「でも、**毎回説明すればいいのでは？.cursorrulesを作る必要はあるの？**」と確認する。
- **デイビッド：** 「毎回説明するのは**時間の無駄**だし、**見落としがある**可能性がある。`.cursorrules`ファイルを作っておくと、**自動で理解**してくれるから、**毎回同じことを説明する必要がなくなる**。それに、**チームで共有**できるから、**一貫性が保てる**よ。」

**パネル3：** アレックスが「次は、どんなことを学ぶんですか？」と聞く。
- **デイビッド：** 「次は、**70%問題**について学ぼう。AIが生成するコードは、**70%くらいしか完成していない**ことがある。残りの30%を完成させる方法を学ぼう。」

**【インフォグラフィックパート】**

**ページ19-20：**
- **この章で学んだこと：**
  - ✅ **なぜ.cursorrulesが必要か**を理解した：毎回同じことを説明するのは時間の無駄、チームで共有できる、一貫性が保てる、など
  - ✅ `.cursorrules`ファイルを作成し、プロジェクトのルールをCursorに教える方法を学んだ
  - ✅ チームで`.cursorrules`を共有する方法を学んだ
  - ✅ **.cursorrulesを作らないとどうなるか**を理解した：毎回同じことを説明する必要がある、見落としがある、チームで共有できない、一貫性が保てない、など
  - ✅ **.cursorrulesを作るとどうなるか**を理解した：毎回同じことを説明する必要がなくなる、チームで共有できる、一貫性が保てる、プロジェクトのルールが明確になる、など

- **.cursorrulesのメリット：**
  1. **毎回同じことを説明する必要がなくなる**（時間が省ける）
  2. **チームで共有できる**（チームメンバー全員が同じルールを使える）
  3. **一貫性が保てる**（プロジェクト全体で同じコードスタイルになる）
  4. **プロジェクトのルールが明確になる**（`.cursorrules`ファイルを見れば、どんなルールがあるかわかる）

- **次の章：** 第13章「70%問題：AI生成コードを完成させる」

---

**確認完了日：** 2025-01-21
